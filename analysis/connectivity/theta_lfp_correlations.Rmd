---
title: "Across Region Theta Power Envelope Correlations"
output: html_document
date: "2023-10-10"
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(
  echo = FALSE,  # don't print the code chunk
  warning = FALSE,  # don't print warnings
  message = FALSE,  # don't print messages
  fig.width = 5,  # set default width of figures
  fig.height = 8,  # set default height of figures
  fig.align = "center",  # always align figure in center
  fig.pos = "H",  # always plot figure at the exact location of the code chunk
  cache = FALSE)   # cache results

### libraries ###
library(tidyverse)
library(ggplot2)
library(magrittr)
library(grid)
library(gtable)
library(RColorBrewer)
library(doParallel)
library(parallel)
library(foreach)
library(here)
library(fs)
library(scales)
library(ggthemr)
library(knitr)
### hand written functions ###
source(path(here(), "R", 'mutate_cond.R'))
source(path(here(), "R", 'clean_behavioral_data.R'))

### plotting helpers ###
ggthemr("light")
getPalette = colorRampPalette(brewer.pal(17, "Set1"))
ghost_colors <- c("#E48DB7","#55BBC8", "#DE0D16")

c25 <- c(
  "dodgerblue2", "#E31A1C", # red
  "green4",
  "#6A3D9A", # purple
  "#FF7F00", # orange
  "black", "gold1",
  "skyblue2", "#FB9A99", # lt pink
  "palegreen2",
  "#CAB2D6", # lt purple
  "#FDBF6F", # lt orange
  "gray70", "khaki2",
  "maroon", "orchid1", "deeppink1", "blue1", "steelblue4",
  "darkturquoise", "green1", "yellow4", "yellow3",
  "darkorange4", "brown"
)

# # ### parallelization ###
# nCores <- 16
# registerDoParallel(nCores)



```

# Overview of Connectivity during Approach-Avoidance

I hypothesize the connectivity profiles to be dynamic, which begs the question of what windows to investigate during the task. Here is a summary of the differetn time windows

* 1 seconds after onset. In the first second after trial onset, we see ramping activity in multiple ROI, including the dlPFC. Understanding the connectivity at this moment could help explain this activity.
* 2 seconds before and after turnaround. Used this for the original SFN submission. Chose to tighten to 1 second before and after around turnaround to get at the connectivity related to the decision to approach vs avoid
* 1 second before turn. True time of the decision. Will be interesting if very different from the before and after analysis


# Onset Analysis - 1 second after onset

## dlPFC 

### dlPFC ~ Hippocampus Onset

```{r count-dlpfc-hc-onset}

all_results <- tibble(case = character(0), percent = numeric(0))

# LL10 hc #
print("LL10")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL10_correlation_onset_wilcox_test_dlpfc_hc.csv"),  
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

hc_counter <- c(sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

# LL12 hc #
print("LL12")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL12_correlation_onset_wilcox_test_dlpfc_hc.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

hc_counter <- c(hc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

# LL13 hc #
print("LL13")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL13_correlation_onset_wilcox_test_dlpfc_hc.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

hc_counter <- c(hc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH021 hc #
print("BJH021")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH021_correlation_onset_wilcox_test_dlpfc_hc.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))
print(table(results$pval_fdr < .05))

hc_counter <- c(hc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH025 hc #
print("BJH025")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH025_correlation_onset_wilcox_test_dlpfc_hc.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

hc_counter <- c(hc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# SLCH002 hc #
print("SLCH002")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "SLCH002_correlation_onset_wilcox_test_dlpfc_hc.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))
print(table(results$pval_fdr < .05))

hc_counter <- c(hc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))



# BJH016 hc #
print("BJH016")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH016_correlation_onset_wilcox_test_dlpfc_hc.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

hc_counter <- c(hc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

print("Total")
table(hc_counter)  


all_results <- rbind(all_results,
                     tibble(
                       case = opts_current$get()$label,
                       percent = mean(hc_counter)
                     ))
```

### dlPFC ~ OFC Onset

```{r count-dlpfc-ofc-onset}


# LL10 ofc #
print("LL10")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL10_correlation_onset_wilcox_test_dlpfc_ofc.csv"),  
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

ofc_counter <- c(sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

# LL12 ofc #
print("LL12")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL12_correlation_onset_wilcox_test_dlpfc_ofc.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

ofc_counter <- c(ofc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

# LL13 ofc #
print("LL13")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL13_correlation_onset_wilcox_test_dlpfc_ofc.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))
print(table(results$pval_fdr < .05))

ofc_counter <- c(ofc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH021 ofc #
print("BJH021")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH021_correlation_onset_wilcox_test_dlpfc_ofc.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

ofc_counter <- c(ofc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH025 ofc #
print("BJH025")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH025_correlation_onset_wilcox_test_dlpfc_ofc.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

ofc_counter <- c(ofc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# SLCH002 ofc #
print("SLCH002")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "SLCH002_correlation_onset_wilcox_test_dlpfc_ofc.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

ofc_counter <- c(ofc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))



# BJH016 ofc #
print("BJH016")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH016_correlation_onset_wilcox_test_dlpfc_ofc.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

ofc_counter <- c(ofc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

print("Total")
table(ofc_counter) 


all_results <- rbind(all_results,                      tibble(                        case = opts_current$get()$label,                        percent = mean(ofc_counter)                      ))

```

### dlPFC ~ Cing Onset

```{r count-dlpfc-cing-onset}


# LL10 cing #
print("LL10")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL10_correlation_onset_wilcox_test_dlpfc_cing.csv"),  
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

cing_counter <- c(sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

# LL12 cing #
print("LL12")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL12_correlation_onset_wilcox_test_dlpfc_cing.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

cing_counter <- c(cing_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

# LL13 cing #
print("LL13")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL13_correlation_onset_wilcox_test_dlpfc_cing.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

cing_counter <- c(cing_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH021 cing #
print("BJH021")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH021_correlation_onset_wilcox_test_dlpfc_cing.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

cing_counter <- c(cing_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH025 cing #
print("BJH025")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH025_correlation_onset_wilcox_test_dlpfc_cing.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

cing_counter <- c(cing_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# SLCH002 cing #
print("SLCH002")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "SLCH002_correlation_onset_wilcox_test_dlpfc_cing.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

cing_counter <- c(cing_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))



# BJH016 cing #
print("BJH016")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH016_correlation_onset_wilcox_test_dlpfc_cing.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

cing_counter <- c(cing_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

print("Total")
table(cing_counter) 


all_results <- rbind(all_results,                      tibble(                        case = opts_current$get()$label,                        percent = mean(cing_counter)                      ))

```

### dlPFC ~ Amyg Onset

```{r count-dlpfc-amyg-onset}


# LL10 amyg #
print("LL10")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL10_correlation_onset_wilcox_test_dlpfc_amyg.csv"),  
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

amyg_counter <- c(sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

# LL12 amyg #
print("LL12")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL12_correlation_onset_wilcox_test_dlpfc_amyg.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

# LL13 amyg #
print("LL13")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL13_correlation_onset_wilcox_test_dlpfc_amyg.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH021 amyg #
print("BJH021")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH021_correlation_onset_wilcox_test_dlpfc_amyg.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH025 amyg #
print("BJH025")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH025_correlation_onset_wilcox_test_dlpfc_amyg.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# SLCH002 amyg #
print("SLCH002")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "SLCH002_correlation_onset_wilcox_test_dlpfc_amyg.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))



# BJH016 amyg #
print("BJH016")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH016_correlation_onset_wilcox_test_dlpfc_amyg.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

print("Total")
table(amyg_counter) 


all_results <- rbind(all_results,                      tibble(                        case = opts_current$get()$label,                        percent = mean(amyg_counter)                      ))
```

### dlPFC ~ Insula Onset

```{r count-dlpfc-insula-onset}


# LL12 insula #
print("LL12")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL12_correlation_onset_wilcox_test_dlpfc_insula.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

insula_counter <- c(sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH021 insula #
print("BJH021")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH021_correlation_onset_wilcox_test_dlpfc_insula.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

insula_counter <- c(insula_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH025 insula #
print("BJH025")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH025_correlation_onset_wilcox_test_dlpfc_insula.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

insula_counter <- c(insula_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# SLCH002 insula #
print("SLCH002")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "SLCH002_correlation_onset_wilcox_test_dlpfc_insula.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

insula_counter <- c(insula_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))



# BJH016 insula #
print("BJH016")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH016_correlation_onset_wilcox_test_dlpfc_insula.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))
print(table(results$pval_fdr < .05))

insula_counter <- c(insula_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

print("Total")
table(insula_counter)  


all_results <- rbind(all_results,                      tibble(                        case = opts_current$get()$label,                        percent = mean(insula_counter)                      ))


```


## DlPFC  -- with no ghost!!!

### dlPFC ~ Hippocampus Onset -- with no ghost!!!

```{r count-hc-onset-noghost}


# LL10 hc #
print("LL10")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL10_correlation_wilcox_test_dlpfc_hc.csv"),  
                    col_names = F, col_types = cols(.default = "c", X1 = col_skip()))
colnames(results) <- c("subject", "elec1", "elec2", "estimate",   "statistics", "pval") 
results <- results  %>%
  filter(!grepl("^X.", pval)) %>%
  mutate(across(c(estimate, statistics, pval), as.numeric)) %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

hc_counter <- c(sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

# LL12 hc #
print("LL12")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL12_correlation_wilcox_test_dlpfc_hc.csv"), 
                    col_names = F, col_types = cols(.default = "c", X1 = col_skip()))
colnames(results) <- c("subject", "elec1", "elec2", "estimate",   "statistics", "pval") 
results <- results  %>%
  filter(!grepl("^X.", pval)) %>%
  mutate(across(c(estimate, statistics, pval), as.numeric)) %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

hc_counter <- c(hc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

# LL13 hc #
print("LL13")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL13_correlation_wilcox_test_dlpfc_hc.csv"), 
                    col_names = F, col_types = cols(.default = "c", X1 = col_skip()))
colnames(results) <- c("subject", "elec1", "elec2", "estimate",   "statistics", "pval") 
results <- results  %>%
  filter(!grepl("^X.", pval)) %>%
  mutate(across(c(estimate, statistics, pval), as.numeric)) %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

hc_counter <- c(hc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH021 hc #
print("BJH021")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH021_correlation_wilcox_test_dlpfc_hc.csv"), 
                    col_names = F, col_types = cols(.default = "c", X1 = col_skip()))
colnames(results) <- c("subject", "elec1", "elec2", "estimate",   "statistics", "pval") 
results <- results  %>%
  filter(!grepl("^X.", pval)) %>%
  mutate(across(c(estimate, statistics, pval), as.numeric)) %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

hc_counter <- c(hc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH025 hc #
print("BJH025")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH025_correlation_wilcox_test_dlpfc_hc.csv"), 
                    col_names = F, col_types = cols(.default = "c", X1 = col_skip()))
colnames(results) <- c("subject", "elec1", "elec2", "estimate",   "statistics", "pval") 
results <- results  %>%
  filter(!grepl("^X.", pval)) %>%
  mutate(across(c(estimate, statistics, pval), as.numeric)) %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

hc_counter <- c(hc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# SLCH002 hc #
print("SLCH002")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "SLCH002_correlation_wilcox_test_dlpfc_hc.csv"), 
                    col_names = F, col_types = cols(.default = "c", X1 = col_skip()))
colnames(results) <- c("subject", "elec1", "elec2", "estimate",   "statistics", "pval") 
results <- results  %>%
  filter(!grepl("^X.", pval)) %>%
  mutate(across(c(estimate, statistics, pval), as.numeric)) %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

hc_counter <- c(hc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))



# BJH016 hc #
print("BJH016")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH016_correlation_wilcox_test_dlpfc_hc.csv"), 
                    col_names = F, col_types = cols(.default = "c", X1 = col_skip()))
colnames(results) <- c("subject", "elec1", "elec2", "estimate",   "statistics", "pval") 
results <- results  %>%
  filter(!grepl("^X.", pval)) %>%
  mutate(across(c(estimate, statistics, pval), as.numeric)) %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

hc_counter <- c(hc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


print("Total")
table(hc_counter)  


all_results <- rbind(all_results,                      tibble(                        case = opts_current$get()$label,                        percent = mean(hc_counter)                      ))
```

### dlPFC ~ Amyg Onset -- with no ghost!!!

```{r count-amyg-onset-noghost}


# LL10 amyg #
print("LL10")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL10_correlation_wilcox_test_dlpfc_amyg.csv"),  
                    col_names = F, col_types = cols(.default = "c", X1 = col_skip()))
colnames(results) <- c("subject", "elec1", "elec2", "estimate",   "statistics", "pval") 
results <- results  %>%
  filter(!grepl("^X.", pval)) %>%
  mutate(across(c(estimate, statistics, pval), as.numeric)) %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

amyg_counter <- c(sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

# LL12 amyg #
print("LL12")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL12_correlation_wilcox_test_dlpfc_amyg.csv"), 
                    col_names = F, col_types = cols(.default = "c", X1 = col_skip()))
colnames(results) <- c("subject", "elec1", "elec2", "estimate",   "statistics", "pval") 
results <- results  %>%
  filter(!grepl("^X.", pval)) %>%
  mutate(across(c(estimate, statistics, pval), as.numeric)) %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

# LL13 amyg #
print("LL13")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL13_correlation_wilcox_test_dlpfc_amyg.csv"), 
                    col_names = F, col_types = cols(.default = "c", X1 = col_skip()))
colnames(results) <- c("subject", "elec1", "elec2", "estimate",   "statistics", "pval") 
results <- results  %>%
  filter(!grepl("^X.", pval)) %>%
  mutate(across(c(estimate, statistics, pval), as.numeric)) %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH021 amyg #
print("BJH021")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH021_correlation_wilcox_test_dlpfc_amyg.csv"), 
                    col_names = F, col_types = cols(.default = "c", X1 = col_skip()))
colnames(results) <- c("subject", "elec1", "elec2", "estimate",   "statistics", "pval") 
results <- results  %>%
  filter(!grepl("^X.", pval)) %>%
  mutate(across(c(estimate, statistics, pval), as.numeric)) %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH025 amyg #
print("BJH025")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH025_correlation_wilcox_test_dlpfc_amyg.csv"), 
                    col_names = F, col_types = cols(.default = "c", X1 = col_skip()))
colnames(results) <- c("subject", "elec1", "elec2", "estimate",   "statistics", "pval") 
results <- results  %>%
  filter(!grepl("^X.", pval)) %>%
  mutate(across(c(estimate, statistics, pval), as.numeric)) %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# SLCH002 amyg #
print("SLCH002")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "SLCH002_correlation_wilcox_test_dlpfc_amyg.csv"), 
                    col_names = F, col_types = cols(.default = "c", X1 = col_skip()))
colnames(results) <- c("subject", "elec1", "elec2", "estimate",   "statistics", "pval") 
results <- results  %>%
  filter(!grepl("^X.", pval)) %>%
  mutate(across(c(estimate, statistics, pval), as.numeric)) %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))



# BJH016 amyg #
print("BJH016")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH016_correlation_wilcox_test_dlpfc_amyg.csv"), 
                    col_names = F, col_types = cols(.default = "c", X1 = col_skip()))
colnames(results) <- c("subject", "elec1", "elec2", "estimate",   "statistics", "pval") 
results <- results  %>%
  filter(!grepl("^X.", pval)) %>%
  mutate(across(c(estimate, statistics, pval), as.numeric)) %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

print("Total")
table(amyg_counter)  


all_results <- rbind(all_results,                      tibble(                        case = opts_current$get()$label,                        percent = mean(amyg_counter)                      ))
```

### dlPFC ~ OFC Onset -- with no ghost!!!

```{r count-ofc-onset-noghost}


# LL10 ofc #
print("LL10")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL10_correlation_wilcox_test_dlpfc_ofc.csv"),  
                    col_names = F, col_types = cols(.default = "c", X1 = col_skip()))
colnames(results) <- c("subject", "elec1", "elec2", "estimate",   "statistics", "pval") 
results <- results  %>%
  filter(!grepl("^X.", pval)) %>%
  mutate(across(c(estimate, statistics, pval), as.numeric)) %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

ofc_counter <- c(sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

# LL12 ofc #
print("LL12")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL12_correlation_wilcox_test_dlpfc_ofc.csv"), 
                    col_names = F, col_types = cols(.default = "c", X1 = col_skip()))
colnames(results) <- c("subject", "elec1", "elec2", "estimate",   "statistics", "pval") 
results <- results  %>%
  filter(!grepl("^X.", pval)) %>%
  mutate(across(c(estimate, statistics, pval), as.numeric)) %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

ofc_counter <- c(ofc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

# LL13 ofc #
print("LL13")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL13_correlation_wilcox_test_dlpfc_ofc.csv"), 
                    col_names = F, col_types = cols(.default = "c", X1 = col_skip()))
colnames(results) <- c("subject", "elec1", "elec2", "estimate",   "statistics", "pval") 
results <- results  %>%
  filter(!grepl("^X.", pval)) %>%
  mutate(across(c(estimate, statistics, pval), as.numeric)) %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

ofc_counter <- c(ofc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH021 ofc #
print("BJH021")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH021_correlation_wilcox_test_dlpfc_ofc.csv"), 
                    col_names = F, col_types = cols(.default = "c", X1 = col_skip()))
colnames(results) <- c("subject", "elec1", "elec2", "estimate",   "statistics", "pval") 
results <- results  %>%
  filter(!grepl("^X.", pval)) %>%
  mutate(across(c(estimate, statistics, pval), as.numeric)) %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

ofc_counter <- c(ofc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH025 ofc #
print("BJH025")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH025_correlation_wilcox_test_dlpfc_ofc.csv"), 
                    col_names = F, col_types = cols(.default = "c", X1 = col_skip()))
colnames(results) <- c("subject", "elec1", "elec2", "estimate",   "statistics", "pval") 
results <- results  %>%
  filter(!grepl("^X.", pval)) %>%
  mutate(across(c(estimate, statistics, pval), as.numeric)) %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

ofc_counter <- c(ofc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# SLCH002 ofc #
print("SLCH002")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "SLCH002_correlation_wilcox_test_dlpfc_ofc.csv"), 
                    col_names = F, col_types = cols(.default = "c", X1 = col_skip()))
colnames(results) <- c("subject", "elec1", "elec2", "estimate",   "statistics", "pval") 
results <- results  %>%
  filter(!grepl("^X.", pval)) %>%
  mutate(across(c(estimate, statistics, pval), as.numeric)) %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

ofc_counter <- c(ofc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))



# BJH016 ofc #
print("BJH016")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH016_correlation_wilcox_test_dlpfc_ofc.csv"), 
                    col_names = F, col_types = cols(.default = "c", X1 = col_skip()))
colnames(results) <- c("subject", "elec1", "elec2", "estimate",   "statistics", "pval") 
results <- results  %>%
  filter(!grepl("^X.", pval)) %>%
  mutate(across(c(estimate, statistics, pval), as.numeric)) %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

ofc_counter <- c(ofc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

print("Total")
table(ofc_counter)  


all_results <- rbind(all_results,                      tibble(                        case = opts_current$get()$label,                        percent = mean(ofc_counter)                      ))

```

### dlPFC ~ Cing Onset -- with no ghost!!!

```{r count-cing-onset-noghost}


# LL10 cing #
print("LL10")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL10_correlation_wilcox_test_dlpfc_cing.csv"),  
                    col_names = F, col_types = cols(.default = "c", X1 = col_skip()))
colnames(results) <- c("subject", "elec1", "elec2", "estimate",   "statistics", "pval") 
results <- results  %>%
  filter(!grepl("^X.", pval)) %>%
  mutate(across(c(estimate, statistics, pval), as.numeric)) %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

cing_counter <- c(sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

# LL12 cing #
print("LL12")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL12_correlation_wilcox_test_dlpfc_cing.csv"), 
                    col_names = F, col_types = cols(.default = "c", X1 = col_skip()))
colnames(results) <- c("subject", "elec1", "elec2", "estimate",   "statistics", "pval") 
results <- results  %>%
  filter(!grepl("^X.", pval)) %>%
  mutate(across(c(estimate, statistics, pval), as.numeric)) %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

cing_counter <- c(cing_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

# LL13 cing #
print("LL13")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL13_correlation_wilcox_test_dlpfc_cing.csv"), 
                    col_names = F, col_types = cols(.default = "c", X1 = col_skip()))
colnames(results) <- c("subject", "elec1", "elec2", "estimate",   "statistics", "pval") 
results <- results  %>%
  filter(!grepl("^X.", pval)) %>%
  mutate(across(c(estimate, statistics, pval), as.numeric)) %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

cing_counter <- c(cing_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH021 cing #
print("BJH021")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH021_correlation_wilcox_test_dlpfc_cing.csv"), 
                    col_names = F, col_types = cols(.default = "c", X1 = col_skip()))
colnames(results) <- c("subject", "elec1", "elec2", "estimate",   "statistics", "pval") 
results <- results  %>%
  filter(!grepl("^X.", pval)) %>%
  mutate(across(c(estimate, statistics, pval), as.numeric)) %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

cing_counter <- c(cing_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH025 cing #
print("BJH025")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH025_correlation_wilcox_test_dlpfc_cing.csv"), 
                    col_names = F, col_types = cols(.default = "c", X1 = col_skip()))
colnames(results) <- c("subject", "elec1", "elec2", "estimate",   "statistics", "pval") 
results <- results  %>%
  filter(!grepl("^X.", pval)) %>%
  mutate(across(c(estimate, statistics, pval), as.numeric)) %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

cing_counter <- c(cing_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# SLCH002 cing #
print("SLCH002")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "SLCH002_correlation_wilcox_test_dlpfc_cing.csv"), 
                    col_names = F, col_types = cols(.default = "c", X1 = col_skip()))
colnames(results) <- c("subject", "elec1", "elec2", "estimate",   "statistics", "pval") 
results <- results  %>%
  filter(!grepl("^X.", pval)) %>%
  mutate(across(c(estimate, statistics, pval), as.numeric)) %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

cing_counter <- c(cing_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))



# BJH016 cing #
print("BJH016")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH016_correlation_wilcox_test_dlpfc_cing.csv"), 
                    col_names = F, col_types = cols(.default = "c", X1 = col_skip()))
colnames(results) <- c("subject", "elec1", "elec2", "estimate",   "statistics", "pval") 
results <- results  %>%
  filter(!grepl("^X.", pval)) %>%
  mutate(across(c(estimate, statistics, pval), as.numeric)) %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

cing_counter <- c(cing_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

print("Total")
table(cing_counter)  


all_results <- rbind(all_results,                      tibble(                        case = opts_current$get()$label,                        percent = mean(cing_counter)                      ))

```

### dlPFC ~ Insula Onset -- with no ghost!!!

```{r count-insula-onset-noghost}



# LL12 insula #
print("LL12")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL12_correlation_wilcox_test_dlpfc_insula.csv"), 
                    col_names = F, col_types = cols(.default = "c", X1 = col_skip()))
colnames(results) <- c("subject", "elec1", "elec2", "estimate",   "statistics", "pval") 
results <- results  %>%
  filter(!grepl("^X.", pval)) %>%
  mutate(across(c(estimate, statistics, pval), as.numeric)) %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

insula_counter <- c(sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH021 insula #
print("BJH021")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH021_correlation_wilcox_test_dlpfc_insula.csv"), 
                    col_names = F, col_types = cols(.default = "c", X1 = col_skip()))
colnames(results) <- c("subject", "elec1", "elec2", "estimate",   "statistics", "pval") 
results <- results  %>%
  filter(!grepl("^X.", pval)) %>%
  mutate(across(c(estimate, statistics, pval), as.numeric)) %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

insula_counter <- c(insula_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH025 insula #
print("BJH025")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH025_correlation_wilcox_test_dlpfc_insula.csv"), 
                    col_names = F, col_types = cols(.default = "c", X1 = col_skip()))
colnames(results) <- c("subject", "elec1", "elec2", "estimate",   "statistics", "pval") 
results <- results  %>%
  filter(!grepl("^X.", pval)) %>%
  mutate(across(c(estimate, statistics, pval), as.numeric)) %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

insula_counter <- c(insula_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# SLCH002 insula #
print("SLCH002")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "SLCH002_correlation_wilcox_test_dlpfc_insula.csv"), 
                    col_names = F, col_types = cols(.default = "c", X1 = col_skip()))
colnames(results) <- c("subject", "elec1", "elec2", "estimate",   "statistics", "pval") 
results <- results  %>%
  filter(!grepl("^X.", pval)) %>%
  mutate(across(c(estimate, statistics, pval), as.numeric)) %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

insula_counter <- c(insula_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))



# BJH016 insula #
print("BJH016")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH016_correlation_wilcox_test_dlpfc_insula.csv"), 
                    col_names = F, col_types = cols(.default = "c", X1 = col_skip()))
colnames(results) <- c("subject", "elec1", "elec2", "estimate",   "statistics", "pval") 
results <- results  %>%
  filter(!grepl("^X.", pval)) %>%
  mutate(across(c(estimate, statistics, pval), as.numeric)) %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

insula_counter <- c(insula_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

print("Total")
table(insula_counter)  


all_results <- rbind(all_results,                      tibble(                        case = opts_current$get()$label,                        percent = mean(insula_counter)                      ))

```

## HC 

### Hippocampus ~ OFC Onset

```{r count-hc-ofc-onset}


# LL10 ofc #
print("LL10")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL10_correlation_onset_wilcox_test_hc_ofc.csv"),  
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

ofc_counter <- c(sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

# LL12 ofc #
print("LL12")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL12_correlation_onset_wilcox_test_hc_ofc.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

ofc_counter <- c(ofc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

# LL13 ofc #
print("LL13")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL13_correlation_onset_wilcox_test_hc_ofc.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))
print(table(results$pval_fdr < .05))

ofc_counter <- c(ofc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH021 ofc #
print("BJH021")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH021_correlation_onset_wilcox_test_hc_ofc.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

ofc_counter <- c(ofc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH025 ofc #
print("BJH025")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH025_correlation_onset_wilcox_test_hc_ofc.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

ofc_counter <- c(ofc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# SLCH002 ofc #
print("SLCH002")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "SLCH002_correlation_onset_wilcox_test_hc_ofc.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

ofc_counter <- c(ofc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))



# BJH016 ofc #
print("BJH016")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH016_correlation_onset_wilcox_test_hc_ofc.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

ofc_counter <- c(ofc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

print("Total")
table(ofc_counter)  


all_results <- rbind(all_results,                      tibble(                        case = opts_current$get()$label,                        percent = mean(ofc_counter)                      ))

```

### Hippocampus ~ Cing Onset

```{r count-hc-cing-onset}


# LL10 cing #
print("LL10")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL10_correlation_onset_wilcox_test_hc_cing.csv"),  
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

cing_counter <- c(sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

# LL12 cing #
print("LL12")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL12_correlation_onset_wilcox_test_hc_cing.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

cing_counter <- c(cing_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

# LL13 cing #
print("LL13")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL13_correlation_onset_wilcox_test_hc_cing.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

cing_counter <- c(cing_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH021 cing #
print("BJH021")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH021_correlation_onset_wilcox_test_hc_cing.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

cing_counter <- c(cing_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH025 cing #
print("BJH025")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH025_correlation_onset_wilcox_test_hc_cing.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

cing_counter <- c(cing_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# SLCH002 cing #
print("SLCH002")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "SLCH002_correlation_onset_wilcox_test_hc_cing.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

cing_counter <- c(cing_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))



# BJH016 cing #
print("BJH016")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH016_correlation_onset_wilcox_test_hc_cing.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

cing_counter <- c(cing_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

print("Total")
table(cing_counter)  


all_results <- rbind(all_results,                      tibble(                        case = opts_current$get()$label,                        percent = mean(cing_counter)                      ))

```

### Hippocampus ~ Amyg Onset

```{r count-hc-amyg-onset}


# LL10 amyg #
print("LL10")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL10_correlation_onset_wilcox_test_hc_amyg.csv"),  
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

amyg_counter <- c(sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

# LL12 amyg #
print("LL12")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL12_correlation_onset_wilcox_test_hc_amyg.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

# LL13 amyg #
print("LL13")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL13_correlation_onset_wilcox_test_hc_amyg.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH021 amyg #
print("BJH021")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH021_correlation_onset_wilcox_test_hc_amyg.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH025 amyg #
print("BJH025")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH025_correlation_onset_wilcox_test_hc_amyg.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# SLCH002 amyg #
print("SLCH002")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "SLCH002_correlation_onset_wilcox_test_hc_amyg.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))



# BJH016 amyg #
print("BJH016")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH016_correlation_onset_wilcox_test_hc_amyg.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

print("Total")
table(amyg_counter)  


all_results <- rbind(all_results,                      tibble(                        case = opts_current$get()$label,                        percent = mean(amyg_counter)                      ))
```

### Hippocampus ~ Insula Onset

```{r count-hc-insula-onset}



# LL12 insula #
print("LL12")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL12_correlation_onset_wilcox_test_hc_insula.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

insula_counter <- c(sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH021 insula #
print("BJH021")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH021_correlation_onset_wilcox_test_hc_insula.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

insula_counter <- c(insula_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH025 insula #
print("BJH025")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH025_correlation_onset_wilcox_test_hc_insula.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

insula_counter <- c(insula_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# SLCH002 insula #
print("SLCH002")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "SLCH002_correlation_onset_wilcox_test_hc_insula.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

insula_counter <- c(insula_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))



# BJH016 insula #
print("BJH016")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH016_correlation_onset_wilcox_test_hc_insula.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))
print(table(results$pval_fdr < .05))

insula_counter <- c(insula_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

print("Total")
table(insula_counter) 


all_results <- rbind(all_results,                      tibble(                        case = opts_current$get()$label,                        percent = mean(insula_counter)                      ))

```

## OFC 

### OFC ~ Cing Onset

```{r count-ofc-cing-onset}


# LL10 cing #
print("LL10")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL10_correlation_onset_wilcox_test_ofc_cing.csv"),  
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

cing_counter <- c(sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

# LL12 cing #
print("LL12")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL12_correlation_onset_wilcox_test_ofc_cing.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

cing_counter <- c(cing_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

# LL13 cing #
print("LL13")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL13_correlation_onset_wilcox_test_ofc_cing.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

cing_counter <- c(cing_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH021 cing #
print("BJH021")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH021_correlation_onset_wilcox_test_ofc_cing.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

cing_counter <- c(cing_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH025 cing #
print("BJH025")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH025_correlation_onset_wilcox_test_ofc_cing.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

cing_counter <- c(cing_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# SLCH002 cing #
print("SLCH002")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "SLCH002_correlation_onset_wilcox_test_ofc_cing.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

cing_counter <- c(cing_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))



# BJH016 cing #
# print("BJH016")
# results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH016_correlation_onset_wilcox_test_ofc_cing.csv"), 
#                     col_names = T, col_types = cols(.default = "c", X1 = col_skip()))
# 
# results <- results %>%
#   mutate(pval_fdr = p.adjust(pval, "fdr"))
# 
# print(table(results$pval_fdr < .05))
# 
# cing_counter <- c(cing_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

print("Total")
table(cing_counter)  


all_results <- rbind(all_results,                      tibble(                        case = opts_current$get()$label,                        percent = mean(cing_counter)                      ))

```

### OFC ~ Amyg Onset

```{r count-ofc-amyg-onset}


# LL10 amyg #
print("LL10")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL10_correlation_onset_wilcox_test_ofc_amyg.csv"),  
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

amyg_counter <- c(sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

# LL12 amyg #
print("LL12")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL12_correlation_onset_wilcox_test_ofc_amyg.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

# LL13 amyg #
print("LL13")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL13_correlation_onset_wilcox_test_ofc_amyg.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH021 amyg #
print("BJH021")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH021_correlation_onset_wilcox_test_ofc_amyg.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH025 amyg #
print("BJH025")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH025_correlation_onset_wilcox_test_ofc_amyg.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# SLCH002 amyg #
print("SLCH002")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "SLCH002_correlation_onset_wilcox_test_ofc_amyg.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))



# # BJH016 amyg #
# print("BJH016")
# results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH016_correlation_onset_wilcox_test_ofc_amyg.csv"), 
#                     col_names = T, col_types = cols(.default = "c", X1 = col_skip()))
# 
# results <- results %>%
#   mutate(pval_fdr = p.adjust(pval, "fdr"))
# 
# print(table(results$pval_fdr < .05))
# 
# amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

print("Total")
table(amyg_counter)  


all_results <- rbind(all_results,                      tibble(                        case = opts_current$get()$label,                        percent = mean(amyg_counter)                      ))
```

### OFC ~ Insula Onset

```{r count-ofc-insula-onset}



# LL12 insula #
print("LL12")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL12_correlation_onset_wilcox_test_ofc_insula.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

insula_counter <- c(sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH021 insula #
print("BJH021")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH021_correlation_onset_wilcox_test_ofc_insula.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

insula_counter <- c(insula_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH025 insula #
print("BJH025")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH025_correlation_onset_wilcox_test_ofc_insula.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

insula_counter <- c(insula_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# SLCH002 insula #
print("SLCH002")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "SLCH002_correlation_onset_wilcox_test_ofc_insula.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

insula_counter <- c(insula_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))



# # BJH016 insula #
# print("BJH016")
# results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH016_correlation_onset_wilcox_test_ofc_insula.csv"), 
#                     col_names = T, col_types = cols(.default = "c", X1 = col_skip()))
# 
# results <- results  %>%
#   mutate(pval_fdr = p.adjust(pval, "fdr"))
# print(table(results$pval_fdr < .05))
# 
# insula_counter <- c(insula_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

print("Total")
table(insula_counter)  


all_results <- rbind(all_results,                      tibble(                        case = opts_current$get()$label,                        percent = mean(insula_counter)                      ))

```

## Ant. Cingulate

### Ant. Cingulate ~ Amyg Onset

```{r count-cing-amyg-onset}


# LL10 amyg #
print("LL10")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL10_correlation_onset_wilcox_test_cing_amyg.csv"),  
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

amyg_counter <- c(sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

# LL12 amyg #
print("LL12")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL12_correlation_onset_wilcox_test_cing_amyg.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

# LL13 amyg #
print("LL13")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL13_correlation_onset_wilcox_test_cing_amyg.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH021 amyg #
print("BJH021")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH021_correlation_onset_wilcox_test_cing_amyg.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH025 amyg #
print("BJH025")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH025_correlation_onset_wilcox_test_cing_amyg.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# SLCH002 amyg #
print("SLCH002")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "SLCH002_correlation_onset_wilcox_test_cing_amyg.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))



# # BJH016 amyg #
# print("BJH016")
# results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH016_correlation_onset_wilcox_test_cing_amyg.csv"), 
#                     col_names = T, col_types = cols(.default = "c", X1 = col_skip()))
# 
# results <- results %>%
#   mutate(pval_fdr = p.adjust(pval, "fdr"))
# 
# print(table(results$pval_fdr < .05))
# 
# amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

print("Total")
table(amyg_counter)  


all_results <- rbind(all_results,                      tibble(                        case = opts_current$get()$label,                        percent = mean(amyg_counter)                      ))
```

### Ant. Cingulate ~ Insula Onset

```{r count-cing-insula-onset}



# LL12 insula #
print("LL12")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL12_correlation_onset_wilcox_test_cing_insula.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

insula_counter <- c(sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH021 insula #
print("BJH021")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH021_correlation_onset_wilcox_test_cing_insula.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

insula_counter <- c(insula_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH025 insula #
print("BJH025")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH025_correlation_onset_wilcox_test_cing_insula.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

insula_counter <- c(insula_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# SLCH002 insula #
print("SLCH002")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "SLCH002_correlation_onset_wilcox_test_cing_insula.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

insula_counter <- c(insula_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))



# BJH016 insula #
# print("BJH016")
# results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH016_correlation_onset_wilcox_test_cing_insula.csv"), 
#                     col_names = T, col_types = cols(.default = "c", X1 = col_skip()))
# 
# results <- results  %>%
#   mutate(pval_fdr = p.adjust(pval, "fdr"))
# print(table(results$pval_fdr < .05))
# 
# insula_counter <- c(insula_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

print("Total")
table(insula_counter)  



all_results <- rbind(all_results,                      tibble(                        case = opts_current$get()$label,                        percent = mean(insula_counter)                      ))

```

## Amygdala

### Amygdala ~ Insula Onset

```{r count-amyg-insula-onset}



# LL12 insula #
print("LL12")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL12_correlation_onset_wilcox_test_amyg_insula.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

insula_counter <- c(sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH021 insula #
print("BJH021")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH021_correlation_onset_wilcox_test_amyg_insula.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

insula_counter <- c(insula_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH025 insula #
print("BJH025")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH025_correlation_onset_wilcox_test_amyg_insula.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

insula_counter <- c(insula_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# SLCH002 insula #
print("SLCH002")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "SLCH002_correlation_onset_wilcox_test_amyg_insula.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

insula_counter <- c(insula_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))



# BJH016 insula #
print("BJH016")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH016_correlation_onset_wilcox_test_amyg_insula.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))
print(table(results$pval_fdr < .05))

insula_counter <- c(insula_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

print("Total")
table(insula_counter) 


all_results <- rbind(all_results,                      tibble(                        case = opts_current$get()$label,                        percent = mean(insula_counter)                      ))

```

<!-- # Before Turn Analysis - 1 second before Turn only -->

<!-- ## dlPFC  -->

<!-- ### dlPFC ~ Hippocampus before_only -->

<!-- ```{r count-dlpfc-hc-before_only} -->


<!-- # LL10 hc # -->
<!-- print("LL10") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL10_correlation_before_only_wilcox_test_dlpfc_hc.csv"),   -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- hc_counter <- c(sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->

<!-- # LL12 hc # -->
<!-- print("LL12") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL12_correlation_before_only_wilcox_test_dlpfc_hc.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- hc_counter <- c(hc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->

<!-- # LL13 hc # -->
<!-- print("LL13") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL13_correlation_before_only_wilcox_test_dlpfc_hc.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- hc_counter <- c(hc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->


<!-- # BJH021 hc # -->
<!-- print("BJH021") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH021_correlation_before_only_wilcox_test_dlpfc_hc.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->
<!-- print(table(results$pval_fdr < .05)) -->

<!-- hc_counter <- c(hc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->


<!-- # BJH025 hc # -->
<!-- print("BJH025") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH025_correlation_before_only_wilcox_test_dlpfc_hc.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results  %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- hc_counter <- c(hc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->


<!-- # SLCH002 hc # -->
<!-- print("SLCH002") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "SLCH002_correlation_before_only_wilcox_test_dlpfc_hc.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->
<!-- print(table(results$pval_fdr < .05)) -->

<!-- hc_counter <- c(hc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->



<!-- # BJH016 hc # -->
<!-- print("BJH016") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH016_correlation_before_only_wilcox_test_dlpfc_hc.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- hc_counter <- c(hc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->

<!-- print("Total") -->
<!-- table(hc_counter)   -->


<!-- all_results <- rbind(all_results,                      tibble(                        case = opts_current$get()$label,                        percent = mean(hc_counter)                      )) -->

<!-- ``` -->

<!-- ### dlPFC ~ OFC before_only -->

<!-- ```{r count-dlpfc-ofc-before_only} -->


<!-- # LL10 ofc # -->
<!-- print("LL10") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL10_correlation_before_only_wilcox_test_dlpfc_ofc.csv"),   -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- ofc_counter <- c(sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->

<!-- # LL12 ofc # -->
<!-- print("LL12") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL12_correlation_before_only_wilcox_test_dlpfc_ofc.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- ofc_counter <- c(ofc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->

<!-- # LL13 ofc # -->
<!-- print("LL13") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL13_correlation_before_only_wilcox_test_dlpfc_ofc.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->
<!-- print(table(results$pval_fdr < .05)) -->

<!-- ofc_counter <- c(ofc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->


<!-- # BJH021 ofc # -->
<!-- print("BJH021") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH021_correlation_before_only_wilcox_test_dlpfc_ofc.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- ofc_counter <- c(ofc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->


<!-- # BJH025 ofc # -->
<!-- print("BJH025") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH025_correlation_before_only_wilcox_test_dlpfc_ofc.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- ofc_counter <- c(ofc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->


<!-- # SLCH002 ofc # -->
<!-- print("SLCH002") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "SLCH002_correlation_before_only_wilcox_test_dlpfc_ofc.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- ofc_counter <- c(ofc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->



<!-- # BJH016 ofc # -->
<!-- print("BJH016") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH016_correlation_before_only_wilcox_test_dlpfc_ofc.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- ofc_counter <- c(ofc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->

<!-- print("Total") -->
<!-- table(ofc_counter)   -->



<!-- all_results <- rbind(all_results,                      tibble(                        case = opts_current$get()$label,                        percent = mean(ofc_counter)                      )) -->

<!-- ``` -->

<!-- ### dlPFC ~ Cing before_only -->

<!-- ```{r count-dlpfc-cing-before_only} -->


<!-- # LL10 cing # -->
<!-- print("LL10") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL10_correlation_before_only_wilcox_test_dlpfc_cing.csv"),   -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- cing_counter <- c(sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->

<!-- # LL12 cing # -->
<!-- print("LL12") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL12_correlation_before_only_wilcox_test_dlpfc_cing.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- cing_counter <- c(cing_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->

<!-- # LL13 cing # -->
<!-- print("LL13") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL13_correlation_before_only_wilcox_test_dlpfc_cing.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- cing_counter <- c(cing_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->


<!-- # BJH021 cing # -->
<!-- print("BJH021") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH021_correlation_before_only_wilcox_test_dlpfc_cing.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- cing_counter <- c(cing_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->


<!-- # BJH025 cing # -->
<!-- print("BJH025") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH025_correlation_before_only_wilcox_test_dlpfc_cing.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- cing_counter <- c(cing_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->


<!-- # SLCH002 cing # -->
<!-- print("SLCH002") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "SLCH002_correlation_before_only_wilcox_test_dlpfc_cing.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- cing_counter <- c(cing_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->



<!-- # BJH016 cing # -->
<!-- print("BJH016") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH016_correlation_before_only_wilcox_test_dlpfc_cing.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- cing_counter <- c(cing_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->

<!-- print("Total") -->
<!-- table(cing_counter)   -->


<!-- all_results <- rbind(all_results,                      tibble(                        case = opts_current$get()$label,                        percent = mean(cing_counter)                      )) -->

<!-- ``` -->

<!-- ### dlPFC ~ Amyg before_only -->

<!-- ```{r count-dlpfc-amyg-before_only} -->


<!-- # LL10 amyg # -->
<!-- print("LL10") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL10_correlation_before_only_wilcox_test_dlpfc_amyg.csv"),   -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- amyg_counter <- c(sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->

<!-- # LL12 amyg # -->
<!-- print("LL12") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL12_correlation_before_only_wilcox_test_dlpfc_amyg.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->

<!-- # LL13 amyg # -->
<!-- print("LL13") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL13_correlation_before_only_wilcox_test_dlpfc_amyg.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->


<!-- # BJH021 amyg # -->
<!-- print("BJH021") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH021_correlation_before_only_wilcox_test_dlpfc_amyg.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->


<!-- # BJH025 amyg # -->
<!-- print("BJH025") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH025_correlation_before_only_wilcox_test_dlpfc_amyg.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->


<!-- # SLCH002 amyg # -->
<!-- print("SLCH002") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "SLCH002_correlation_before_only_wilcox_test_dlpfc_amyg.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->



<!-- # BJH016 amyg # -->
<!-- print("BJH016") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH016_correlation_before_only_wilcox_test_dlpfc_amyg.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->

<!-- print("Total") -->
<!-- table(amyg_counter)   -->


<!-- all_results <- rbind(all_results,                      tibble(                        case = opts_current$get()$label,                        percent = mean(amyg_counter)                      )) -->
<!-- ``` -->

<!-- ### dlPFC ~ Insula before_only -->

<!-- ```{r count-dlpfc-insula-before_only} -->



<!-- # LL12 insula # -->
<!-- print("LL12") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL12_correlation_before_only_wilcox_test_dlpfc_insula.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- insula_counter <- c(sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->


<!-- # BJH021 insula # -->
<!-- print("BJH021") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH021_correlation_before_only_wilcox_test_dlpfc_insula.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- insula_counter <- c(insula_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->


<!-- # BJH025 insula # -->
<!-- print("BJH025") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH025_correlation_before_only_wilcox_test_dlpfc_insula.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- insula_counter <- c(insula_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->


<!-- # SLCH002 insula # -->
<!-- print("SLCH002") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "SLCH002_correlation_before_only_wilcox_test_dlpfc_insula.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- insula_counter <- c(insula_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->



<!-- # BJH016 insula # -->
<!-- print("BJH016") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH016_correlation_before_only_wilcox_test_dlpfc_insula.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- insula_counter <- c(insula_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->

<!-- print("Total") -->
<!-- table(insula_counter)   -->


<!-- all_results <- rbind(all_results,                      tibble(                        case = opts_current$get()$label,                        percent = mean(insula_counter)                      )) -->
<!-- ``` -->
<!-- ## HC  -->

<!-- ### Hippocampus ~ OFC before_only -->

<!-- ```{r count-hc-ofc-before_only} -->


<!-- # LL10 ofc # -->
<!-- print("LL10") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL10_correlation_before_only_wilcox_test_hc_ofc.csv"),   -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- ofc_counter <- c(sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->

<!-- # LL12 ofc # -->
<!-- print("LL12") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL12_correlation_before_only_wilcox_test_hc_ofc.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- ofc_counter <- c(ofc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->

<!-- # LL13 ofc # -->
<!-- print("LL13") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL13_correlation_before_only_wilcox_test_hc_ofc.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->
<!-- print(table(results$pval_fdr < .05)) -->

<!-- ofc_counter <- c(ofc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->


<!-- # BJH021 ofc # -->
<!-- print("BJH021") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH021_correlation_before_only_wilcox_test_hc_ofc.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- ofc_counter <- c(ofc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->


<!-- # BJH025 ofc # -->
<!-- print("BJH025") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH025_correlation_before_only_wilcox_test_hc_ofc.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- ofc_counter <- c(ofc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->


<!-- # SLCH002 ofc # -->
<!-- print("SLCH002") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "SLCH002_correlation_before_only_wilcox_test_hc_ofc.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- ofc_counter <- c(ofc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->



<!-- # BJH016 ofc # -->
<!-- print("BJH016") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH016_correlation_before_only_wilcox_test_hc_ofc.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- ofc_counter <- c(ofc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->

<!-- print("Total") -->
<!-- table(ofc_counter)   -->
<!-- all_results <- rbind(all_results,                      tibble(                        case = opts_current$get()$label,                        percent = mean(ofc_counter)                      )) -->

<!-- ``` -->

<!-- ### Hippocampus ~ Cing before_only -->

<!-- ```{r count-hc-cing-before_only} -->


<!-- # LL10 cing # -->
<!-- print("LL10") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL10_correlation_before_only_wilcox_test_hc_cing.csv"),   -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- cing_counter <- c(sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->

<!-- # LL12 cing # -->
<!-- print("LL12") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL12_correlation_before_only_wilcox_test_hc_cing.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- cing_counter <- c(cing_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->

<!-- # LL13 cing # -->
<!-- print("LL13") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL13_correlation_before_only_wilcox_test_hc_cing.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- cing_counter <- c(cing_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->


<!-- # BJH021 cing # -->
<!-- print("BJH021") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH021_correlation_before_only_wilcox_test_hc_cing.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- cing_counter <- c(cing_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->


<!-- # BJH025 cing # -->
<!-- print("BJH025") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH025_correlation_before_only_wilcox_test_hc_cing.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- cing_counter <- c(cing_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->


<!-- # SLCH002 cing # -->
<!-- print("SLCH002") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "SLCH002_correlation_before_only_wilcox_test_hc_cing.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- cing_counter <- c(cing_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->



<!-- # BJH016 cing # -->
<!-- print("BJH016") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH016_correlation_before_only_wilcox_test_hc_cing.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- cing_counter <- c(cing_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->

<!-- print("Total") -->
<!-- table(cing_counter)   -->
<!-- all_results <- rbind(all_results,                      tibble(                        case = opts_current$get()$label,                        percent = mean(cing_counter)                      )) -->

<!-- ``` -->

<!-- ### Hippocampus ~ Amyg before_only -->

<!-- ```{r count-hc-amyg-before_only} -->


<!-- # LL10 amyg # -->
<!-- print("LL10") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL10_correlation_before_only_wilcox_test_hc_amyg.csv"),   -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- amyg_counter <- c(sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->

<!-- # LL12 amyg # -->
<!-- print("LL12") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL12_correlation_before_only_wilcox_test_hc_amyg.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->

<!-- # LL13 amyg # -->
<!-- print("LL13") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL13_correlation_before_only_wilcox_test_hc_amyg.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->


<!-- # BJH021 amyg # -->
<!-- print("BJH021") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH021_correlation_before_only_wilcox_test_hc_amyg.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->


<!-- # BJH025 amyg # -->
<!-- print("BJH025") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH025_correlation_before_only_wilcox_test_hc_amyg.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->


<!-- # SLCH002 amyg # -->
<!-- print("SLCH002") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "SLCH002_correlation_before_only_wilcox_test_hc_amyg.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->



<!-- # BJH016 amyg # -->
<!-- print("BJH016") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH016_correlation_before_only_wilcox_test_hc_amyg.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->

<!-- print("Total") -->
<!-- table(amyg_counter)   -->
<!-- all_results <- rbind(all_results,                      tibble(                        case = opts_current$get()$label,                        percent = mean(amyg_counter)                      )) -->
<!-- ``` -->

<!-- ### Hippocampus ~ Insula before_only -->

<!-- ```{r count-hc-insula-before_only} -->



<!-- # LL12 insula # -->
<!-- print("LL12") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL12_correlation_before_only_wilcox_test_hc_insula.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results  %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- insula_counter <- c(sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->


<!-- # BJH021 insula # -->
<!-- print("BJH021") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH021_correlation_before_only_wilcox_test_hc_insula.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results  %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- insula_counter <- c(insula_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->


<!-- # BJH025 insula # -->
<!-- print("BJH025") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH025_correlation_before_only_wilcox_test_hc_insula.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- insula_counter <- c(insula_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->


<!-- # SLCH002 insula # -->
<!-- print("SLCH002") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "SLCH002_correlation_before_only_wilcox_test_hc_insula.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- insula_counter <- c(insula_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->



<!-- # BJH016 insula # -->
<!-- print("BJH016") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH016_correlation_before_only_wilcox_test_hc_insula.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results  %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->
<!-- print(table(results$pval_fdr < .05)) -->

<!-- insula_counter <- c(insula_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->

<!-- print("Total") -->
<!-- table(insula_counter)   -->
<!-- all_results <- rbind(all_results,                      tibble(                        case = opts_current$get()$label,                        percent = mean(insula_counter)                      )) -->

<!-- ``` -->

<!-- ## OFC  -->

<!-- ### OFC ~ Cing before_only -->

<!-- ```{r count-ofc-cing-before_only} -->


<!-- # LL10 cing # -->
<!-- print("LL10") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL10_correlation_before_only_wilcox_test_ofc_cing.csv"),   -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- cing_counter <- c(sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->

<!-- # LL12 cing # -->
<!-- print("LL12") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL12_correlation_before_only_wilcox_test_ofc_cing.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- cing_counter <- c(cing_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->

<!-- # LL13 cing # -->
<!-- print("LL13") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL13_correlation_before_only_wilcox_test_ofc_cing.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- cing_counter <- c(cing_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->


<!-- # BJH021 cing # -->
<!-- print("BJH021") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH021_correlation_before_only_wilcox_test_ofc_cing.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- cing_counter <- c(cing_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->


<!-- # BJH025 cing # -->
<!-- print("BJH025") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH025_correlation_before_only_wilcox_test_ofc_cing.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- cing_counter <- c(cing_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->


<!-- # SLCH002 cing # -->
<!-- print("SLCH002") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "SLCH002_correlation_before_only_wilcox_test_ofc_cing.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- cing_counter <- c(cing_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->



<!-- # BJH016 cing # -->
<!-- # print("BJH016") -->
<!-- # results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH016_correlation_before_only_wilcox_test_ofc_cing.csv"),  -->
<!-- #                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->
<!-- #  -->
<!-- # results <- results %>% -->
<!-- #   mutate(pval_fdr = p.adjust(pval, "fdr")) -->
<!-- #  -->
<!-- # print(table(results$pval_fdr < .05)) -->
<!-- #  -->
<!-- # cing_counter <- c(cing_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->

<!-- print("Total") -->
<!-- table(cing_counter)   -->
<!-- all_results <- rbind(all_results,                      tibble(                        case = opts_current$get()$label,                        percent = mean(cing_counter)                      )) -->

<!-- ``` -->

<!-- ### OFC ~ Amyg before_only -->

<!-- ```{r count-ofc-amyg-before_only} -->


<!-- # LL10 amyg # -->
<!-- print("LL10") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL10_correlation_before_only_wilcox_test_ofc_amyg.csv"),   -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- amyg_counter <- c(sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->

<!-- # LL12 amyg # -->
<!-- print("LL12") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL12_correlation_before_only_wilcox_test_ofc_amyg.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->

<!-- # LL13 amyg # -->
<!-- print("LL13") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL13_correlation_before_only_wilcox_test_ofc_amyg.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->


<!-- # BJH021 amyg # -->
<!-- print("BJH021") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH021_correlation_before_only_wilcox_test_ofc_amyg.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->


<!-- # BJH025 amyg # -->
<!-- print("BJH025") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH025_correlation_before_only_wilcox_test_ofc_amyg.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->


<!-- # SLCH002 amyg # -->
<!-- print("SLCH002") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "SLCH002_correlation_before_only_wilcox_test_ofc_amyg.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->



<!-- # # BJH016 amyg # -->
<!-- # print("BJH016") -->
<!-- # results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH016_correlation_before_only_wilcox_test_ofc_amyg.csv"),  -->
<!-- #                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->
<!-- #  -->
<!-- # results <- results %>% -->
<!-- #   mutate(pval_fdr = p.adjust(pval, "fdr")) -->
<!-- #  -->
<!-- # print(table(results$pval_fdr < .05)) -->
<!-- #  -->
<!-- # amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->

<!-- print("Total") -->
<!-- table(amyg_counter)   -->
<!-- all_results <- rbind(all_results,                      tibble(                        case = opts_current$get()$label,                        percent = mean(amyg_counter)                      )) -->
<!-- ``` -->

<!-- ### OFC ~ Insula before_only -->

<!-- ```{r count-ofc-insula-before_only} -->



<!-- # LL12 insula # -->
<!-- print("LL12") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL12_correlation_before_only_wilcox_test_ofc_insula.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results  %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- insula_counter <- c(sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->


<!-- # BJH021 insula # -->
<!-- print("BJH021") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH021_correlation_before_only_wilcox_test_ofc_insula.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results  %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- insula_counter <- c(insula_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->


<!-- # BJH025 insula # -->
<!-- print("BJH025") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH025_correlation_before_only_wilcox_test_ofc_insula.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- insula_counter <- c(insula_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->


<!-- # SLCH002 insula # -->
<!-- print("SLCH002") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "SLCH002_correlation_before_only_wilcox_test_ofc_insula.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- insula_counter <- c(insula_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->



<!-- # BJH016 insula # -->
<!-- # print("BJH016") -->
<!-- # results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH016_correlation_before_only_wilcox_test_ofc_insula.csv"),  -->
<!-- #                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->
<!-- #  -->
<!-- # results <- results  %>% -->
<!-- #   mutate(pval_fdr = p.adjust(pval, "fdr")) -->
<!-- # print(table(results$pval_fdr < .05)) -->
<!-- #  -->
<!-- # insula_counter <- c(insula_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->

<!-- print("Total") -->
<!-- table(insula_counter)   -->
<!-- all_results <- rbind(all_results,                      tibble(                        case = opts_current$get()$label,                        percent = mean(insula_counter)                      )) -->

<!-- ``` -->

<!-- ## Ant. Cingulate -->

<!-- ### Ant. Cingulate ~ Amyg before_only -->

<!-- ```{r count-cing-amyg-before_only} -->


<!-- # LL10 amyg # -->
<!-- print("LL10") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL10_correlation_before_only_wilcox_test_cing_amyg.csv"),   -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- amyg_counter <- c(sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->

<!-- # LL12 amyg # -->
<!-- print("LL12") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL12_correlation_before_only_wilcox_test_cing_amyg.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->

<!-- # LL13 amyg # -->
<!-- print("LL13") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL13_correlation_before_only_wilcox_test_cing_amyg.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->


<!-- # BJH021 amyg # -->
<!-- print("BJH021") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH021_correlation_before_only_wilcox_test_cing_amyg.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->


<!-- # BJH025 amyg # -->
<!-- print("BJH025") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH025_correlation_before_only_wilcox_test_cing_amyg.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->


<!-- # SLCH002 amyg # -->
<!-- print("SLCH002") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "SLCH002_correlation_before_only_wilcox_test_cing_amyg.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->



<!-- # # BJH016 amyg # -->
<!-- # print("BJH016") -->
<!-- # results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH016_correlation_before_only_wilcox_test_cing_amyg.csv"),  -->
<!-- #                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->
<!-- #  -->
<!-- # results <- results %>% -->
<!-- #   mutate(pval_fdr = p.adjust(pval, "fdr")) -->
<!-- #  -->
<!-- # print(table(results$pval_fdr < .05)) -->
<!-- #  -->
<!-- # amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->

<!-- print("Total") -->
<!-- table(amyg_counter)   -->
<!-- all_results <- rbind(all_results,                      tibble(                        case = opts_current$get()$label,                        percent = mean(amyg_counter)                      )) -->
<!-- ``` -->

<!-- ### Ant. Cingulate ~ Insula before_only -->

<!-- ```{r count-cing-insula-before_only} -->



<!-- # LL12 insula # -->
<!-- print("LL12") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL12_correlation_before_only_wilcox_test_cing_insula.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results  %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- insula_counter <- c(sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->


<!-- # BJH021 insula # -->
<!-- print("BJH021") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH021_correlation_before_only_wilcox_test_cing_insula.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results  %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- insula_counter <- c(insula_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->


<!-- # BJH025 insula # -->
<!-- print("BJH025") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH025_correlation_before_only_wilcox_test_cing_insula.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- insula_counter <- c(insula_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->


<!-- # SLCH002 insula # -->
<!-- print("SLCH002") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "SLCH002_correlation_before_only_wilcox_test_cing_insula.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- insula_counter <- c(insula_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->



<!-- # BJH016 insula # -->
<!-- # print("BJH016") -->
<!-- # results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH016_correlation_before_only_wilcox_test_cing_insula.csv"),  -->
<!-- #                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->
<!-- #  -->
<!-- # results <- results  %>% -->
<!-- #   mutate(pval_fdr = p.adjust(pval, "fdr")) -->
<!-- # print(table(results$pval_fdr < .05)) -->
<!-- #  -->
<!-- # insula_counter <- c(insula_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->

<!-- print("Total") -->
<!-- table(insula_counter)   -->
<!-- all_results <- rbind(all_results,                      tibble(                        case = opts_current$get()$label,                        percent = mean(insula_counter)                      )) -->

<!-- ``` -->

<!-- ## Amygdala -->

<!-- ### Amygdala ~ Insula before_only -->

<!-- ```{r count-amyg-insula-before_only} -->



<!-- # LL12 insula # -->
<!-- print("LL12") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL12_correlation_before_only_wilcox_test_amyg_insula.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results  %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- insula_counter <- c(sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->


<!-- # BJH021 insula # -->
<!-- print("BJH021") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH021_correlation_before_only_wilcox_test_amyg_insula.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results  %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- insula_counter <- c(insula_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->


<!-- # BJH025 insula # -->
<!-- print("BJH025") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH025_correlation_before_only_wilcox_test_amyg_insula.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- insula_counter <- c(insula_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->


<!-- # SLCH002 insula # -->
<!-- print("SLCH002") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "SLCH002_correlation_before_only_wilcox_test_amyg_insula.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->

<!-- print(table(results$pval_fdr < .05)) -->

<!-- insula_counter <- c(insula_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->



<!-- # BJH016 insula # -->
<!-- print("BJH016") -->
<!-- results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH016_correlation_before_only_wilcox_test_amyg_insula.csv"),  -->
<!--                     col_names = T, col_types = cols(.default = "c", X1 = col_skip())) -->

<!-- results <- results  %>% -->
<!--   mutate(pval_fdr = p.adjust(pval, "fdr")) -->
<!-- print(table(results$pval_fdr < .05)) -->

<!-- insula_counter <- c(insula_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05)) -->

<!-- print("Total") -->
<!-- table(insula_counter)   -->
<!-- all_results <- rbind(all_results,                      tibble(                        case = opts_current$get()$label,                        percent = mean(insula_counter)                      )) -->

<!-- ``` -->





# Last Away Analysis - 2 seconds before, 2 seconds after

### dlPFC ~ Hippocampus - Last Away 2 seconds before, 2 seconds after

```{r count-dlpfc-sfn}

dlpfc_counter <- c()

# LL10 dlPFC #
print("LL10")
results <- read.csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis","sfn_submission",
                         "LL10_correlation_wilcox_test_dlpfc.csv"), header = T)
tmp <- results %>%
  filter(X != 1) %>%
  mutate(V5 = as.numeric( V5)) %>%
  mutate(pval_fdr = p.adjust(V5, "fdr"))
table(tmp$pval_fdr < .05)
# sum(as.numeric(tmp$pval_fdr < .05))/nrow(tmp)
dlpfc_counter <- c(dlpfc_counter, tmp$pval_fdr < .05)

# LL12 dlPFC #
print("LL12")
results <- read.csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "sfn_submission","LL12_correlation_wilcox_test_hc_dlpfc.csv"), header = F)
tmp <- results %>%
  filter(!is.na(V1)) %>%
  mutate(V5 = as.numeric( V5)) %>%
  mutate(pval_fdr = p.adjust(V5, "fdr"))
table(tmp$pval_fdr < .05)
# sum(as.numeric(tmp$pval_fdr < .05))/nrow(tmp)
dlpfc_counter <- c(dlpfc_counter, tmp$pval_fdr < .05)

# LL13 dlPFC #
print("LL13")
results <- read.csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "sfn_submission","LL13_correlation_wilcox_test_hc_dlpfc.csv"), header = F)
tmp <- results %>%
  filter(!is.na(V1)) %>%
  mutate(V5 = as.numeric( V5)) %>%
  mutate(pval_fdr = p.adjust(V5, "fdr"))
table(tmp$pval_fdr < .05)
sum(as.numeric(tmp$pval_fdr < .05))/nrow(tmp)
dlpfc_counter <- c(dlpfc_counter, tmp$pval_fdr < .05)

# BJH021 dlPFC #
print("BJH021")
results <- read.csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "sfn_submission","BJH021_correlation_wilcox_test_hc_dlpfc.csv"), header = F)
tmp <- results %>%
  filter(!is.na(V1)) %>%
  mutate(V5 = as.numeric( V5)) %>%
  mutate(pval_fdr = p.adjust(V5, "fdr"))
table(tmp$pval_fdr < .05)
# sum(as.numeric(tmp$pval_fdr < .05))/nrow(tmp)

dlpfc_counter <- c(dlpfc_counter, tmp$pval_fdr < .05)


# BJH025 dlPFC #
print("BJH025")
results <- read.csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "sfn_submission","BJH025_correlation_wilcox_test_hc_dlpfc.csv"), header = F)
tmp <- results %>%
  filter(!is.na(V1)) %>%
  mutate(V5 = as.numeric( V5)) %>%
  mutate(pval_fdr = p.adjust(V5, "fdr"))
table(tmp$pval_fdr < .05)
# sum(as.numeric(tmp$pval_fdr < .05))/nrow(tmp)
dlpfc_counter <- c(dlpfc_counter, tmp$pval_fdr < .05)

# SLCH002 dlPFC #
print("SLCH002")
results <- read.csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "sfn_submission","SLCH002_correlation_wilcox_test_hc_dlpfc.csv"), header = F)
tmp <- results %>%
  filter(!is.na(V1)) %>%
  mutate(V5 = as.numeric( V5)) %>%
  mutate(pval_fdr = p.adjust(V5, "fdr"))
table(tmp$pval_fdr < .05)
# sum(as.numeric(tmp$pval_fdr < .05))/nrow(tmp)
dlpfc_counter <- c(dlpfc_counter, tmp$pval_fdr < .05)


# BJH016 dlPFC #
print("BJH016")
results <- read.csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "sfn_submission","BJH016_correlation_wilcox_test_hc_dlpfc.csv"), header = F)
tmp <- results %>%
  filter(!is.na(V1) & V1 != 1) %>%
  mutate(V5 = as.numeric( V5)) %>%
  mutate(pval_fdr = p.adjust(V5, "fdr"))
table(tmp$pval_fdr < .05)

dlpfc_counter <- c(dlpfc_counter, tmp$pval_fdr < .05)

print("Total")
table(dlpfc_counter)  


all_results <- rbind(all_results,
                     tibble(case = opts_current$get()$label,
                            percent = mean(dlpfc_counter)))


```

### Ant. Cingulate ~ Hippocampus - Last Away 2 seconds before, 2 seconds after

```{r count-cing-sfn}



# LL10 cing #
results <- read.csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "sfn_submission","LL10_correlation_wilcox_test_cing.csv"), header = T)
tmp <- results  %>%
  filter(X != 1) %>%
  mutate(V5 = as.numeric( V5)) %>%
  mutate(pval_fdr = p.adjust(V5, "fdr"))

table(tmp$pval_fdr < .05)

cing_counter <- c(tmp$pval_fdr < .05)

# LL12 cing #
results <- read.csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "sfn_submission","LL12_correlation_wilcox_test_hc_cing.csv"), header = F)
tmp <- results %>%
  filter(!is.na(V1)) %>%
  mutate(V5 = as.numeric( V5)) %>%
  mutate(pval_fdr = p.adjust(V5, "fdr"))
table(tmp$pval_fdr < .05)

cing_counter <- c(cing_counter, tmp$pval_fdr < .05)

# LL13 cing #
results <- read.csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "sfn_submission","LL13_correlation_wilcox_test_hc_cing.csv"), header = F)
tmp <- results %>%
  filter(!is.na(V1)) %>%
  mutate(V5 = as.numeric( V5)) %>%
  mutate(pval_fdr = p.adjust(V5, "fdr"))
table(tmp$pval_fdr < .05)
cing_counter <- c(cing_counter, tmp$pval_fdr < .05)

# BJH021 cing #
results <- read.csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "sfn_submission","BJH021_correlation_wilcox_test_hc_cing.csv"), header = F)
tmp <- results %>%
  filter(!is.na(V1)) %>%
  mutate(V5 = as.numeric( V5)) %>%
  mutate(pval_fdr = p.adjust(V5, "fdr"))
table(tmp$pval_fdr < .05)
cing_counter <- c(cing_counter, tmp$pval_fdr < .05)

# BJH025 cing #
results <- read.csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "sfn_submission","BJH025_correlation_wilcox_test_hc_cing.csv"), header = F)
tmp <- results %>%
  filter(!is.na(V1)) %>%
  mutate(V5 = as.numeric( V5)) %>%
  mutate(pval_fdr = p.adjust(V5, "fdr"))
table(tmp$pval_fdr < .05)
cing_counter <- c(cing_counter, tmp$pval_fdr < .05)

# SLCH002 cing #
results <- read.csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "sfn_submission","SLCH002_correlation_wilcox_test_hc_cing.csv"), header = T)
tmp <- results %>%
  filter(X != 1) %>%
  mutate(V5 = as.numeric( V5)) %>%
  mutate(pval_fdr = p.adjust(V5, "fdr"))
table(tmp$pval_fdr < .05)
cing_counter <- c(cing_counter, tmp$pval_fdr < .05)


# BJH016 cing #
results <- read.csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "sfn_submission","BJH016_correlation_wilcox_test_hc_cing.csv"), header = F)
tmp <- results %>%
  filter(!is.na(V1)) %>%
  mutate(V5 = as.numeric( V5)) %>%
  mutate(pval_fdr = p.adjust(V5, "fdr"))
table(tmp$pval_fdr < .05)
cing_counter <- c(cing_counter, tmp$pval_fdr < .05)

print("Total")
table(cing_counter)  
all_results <- rbind(all_results,                      tibble(                        case = opts_current$get()$label,                        percent = mean(cing_counter)                      ))
```

### OFC ~ Hippocampus - Last Away 2 seconds before, 2 seconds after

```{r count-ofc-sfn}

# LL10 ofc #
results <- read.csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "sfn_submission","LL10_correlation_wilcox_test_hc_ofc.csv"), header = F)
tmp <- results  %>%
  filter(!is.na(V1)) %>%
  mutate(V5 = as.numeric( V5)) %>%
  mutate(pval_fdr = p.adjust(V5, "fdr"))
table(tmp$pval_fdr < .05)

table(tmp$pval_fdr < .05)

ofc_counter <- c(tmp$pval_fdr < .05)

# LL12 ofc #
results <- read.csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "sfn_submission","LL12_correlation_wilcox_test_hc_ofc.csv"), header = F)
tmp <- results %>%
  filter(!is.na(V1)) %>%
  mutate(V5 = as.numeric( V5)) %>%
  mutate(pval_fdr = p.adjust(V5, "fdr"))
table(tmp$pval_fdr < .05)

ofc_counter <- c(ofc_counter, tmp$pval_fdr < .05)

# LL13 ofc #
results <- read.csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "sfn_submission","LL13_correlation_wilcox_test_hc_ofc.csv"), header = F)
tmp <- results %>%
  filter(!is.na(V1)) %>%
  mutate(V5 = as.numeric( V5)) %>%
  mutate(pval_fdr = p.adjust(V5, "fdr"))
table(tmp$pval_fdr < .05)
ofc_counter <- c(ofc_counter, tmp$pval_fdr < .05)

# BJH021 ofc #
results <- read.csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "sfn_submission","BJH021_correlation_wilcox_test_hc_ofc.csv"), header = F)
tmp <- results %>%
  filter(!is.na(V1)) %>%
  mutate(V5 = as.numeric( V5)) %>%
  mutate(pval_fdr = p.adjust(V5, "fdr"))
table(tmp$pval_fdr < .05)
ofc_counter <- c(ofc_counter, tmp$pval_fdr < .05)

# BJH025 ofc #
results <- read.csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "sfn_submission","BJH025_correlation_wilcox_test_hc_ofc.csv"), header = F)
tmp <- results %>%
  filter(!is.na(V1)) %>%
  mutate(V5 = as.numeric( V5)) %>%
  mutate(pval_fdr = p.adjust(V5, "fdr"))
table(tmp$pval_fdr < .05)
ofc_counter <- c(ofc_counter, tmp$pval_fdr < .05)

# SLCH002 ofc #
results <- read.csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "sfn_submission","SLCH002_correlation_wilcox_test_hc_ofc.csv"), header = F)
tmp <- results %>%
  filter(!is.na(V1)) %>%
  mutate(V5 = as.numeric( V5)) %>%
  mutate(pval_fdr = p.adjust(V5, "fdr"))
table(tmp$pval_fdr < .05)
ofc_counter <- c(ofc_counter, tmp$pval_fdr < .05)


# BJH016 ofc #
results <- read.csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "sfn_submission","BJH016_correlation_wilcox_test_hc_ofc.csv"), header = F)
tmp <- results %>%
  filter(!is.na(V1)) %>%
  mutate(V5 = as.numeric( V5)) %>%
  mutate(pval_fdr = p.adjust(V5, "fdr"))
table(tmp$pval_fdr < .05)
ofc_counter <- c(ofc_counter, tmp$pval_fdr < .05)

print("Total")
table(ofc_counter)  
all_results <- rbind(all_results,                      tibble(                        case = opts_current$get()$label,                        percent = mean(ofc_counter)                      ))

```

### Amygdala ~ Hippocampus - Last Away 2 seconds before, 2 seconds after

```{r count-amyg-sfn}

# LL10 amyg #
results <- read.csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "sfn_submission","LL10_correlation_wilcox_test_hc_amyg.csv"), header = F)
tmp <- results  %>%
  filter(!is.na(V1)) %>%
  mutate(V5 = as.numeric( V5)) %>%
  mutate(pval_fdr = p.adjust(V5, "fdr"))
table(tmp$pval_fdr < .05)

table(tmp$pval_fdr < .05)

amyg_counter <- c(tmp$pval_fdr < .05)

# LL12 amyg #
results <- read.csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "sfn_submission","LL12_correlation_wilcox_test_hc_amyg.csv"), header = F)
tmp <- results %>%
  filter(!is.na(V1)) %>%
  mutate(V5 = as.numeric( V5)) %>%
  mutate(pval_fdr = p.adjust(V5, "fdr"))
table(tmp$pval_fdr < .05)

amyg_counter <- c(amyg_counter, tmp$pval_fdr < .05)

# LL13 amyg #
results <- read.csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "sfn_submission","LL13_correlation_wilcox_test_hc_amyg.csv"), header = F)
tmp <- results %>%
  filter(!is.na(V1)) %>%
  mutate(V5 = as.numeric( V5)) %>%
  mutate(pval_fdr = p.adjust(V5, "fdr"))
table(tmp$pval_fdr < .05)
amyg_counter <- c(amyg_counter, tmp$pval_fdr < .05)

# BJH021 amyg #
results <- read.csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "sfn_submission","BJH021_correlation_wilcox_test_hc_amyg.csv"), header = F)
tmp <- results %>%
  filter(!is.na(V1)) %>%
  mutate(V5 = as.numeric( V5)) %>%
  mutate(pval_fdr = p.adjust(V5, "fdr"))
table(tmp$pval_fdr < .05)
amyg_counter <- c(amyg_counter, tmp$pval_fdr < .05)

# BJH025 amyg #
results <- read.csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "sfn_submission","BJH025_correlation_wilcox_test_hc_amyg.csv"), header = F)
tmp <- results %>%
  filter(!is.na(V1)) %>%
  mutate(V5 = as.numeric( V5)) %>%
  mutate(pval_fdr = p.adjust(V5, "fdr"))
table(tmp$pval_fdr < .05)
amyg_counter <- c(amyg_counter, tmp$pval_fdr < .05)

# SLCH002 amyg #
results <- read.csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "sfn_submission","SLCH002_correlation_wilcox_test_hc_amyg.csv"), header = F)
tmp <- results %>%
  filter(!is.na(V1)) %>%
  mutate(V5 = as.numeric( V5)) %>%
  mutate(pval_fdr = p.adjust(V5, "fdr"))
table(tmp$pval_fdr < .05)
amyg_counter <- c(amyg_counter, tmp$pval_fdr < .05)


# BJH016 amyg #
results <- read.csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "sfn_submission","BJH016_correlation_wilcox_test_hc_amyg.csv"), header = F)
tmp <- results %>%
  filter(!is.na(V1)) %>%
  mutate(V5 = as.numeric( V5)) %>%
  mutate(pval_fdr = p.adjust(V5, "fdr"))
table(tmp$pval_fdr < .05)
amyg_counter <- c(amyg_counter, tmp$pval_fdr < .05)

print("Total")
table(amyg_counter)  
all_results <- rbind(all_results,                      tibble(                        case = opts_current$get()$label,                        percent = mean(amyg_counter)                      ))
```

### Insula ~ Hippocampus - Last Away 2 seconds before, 2 seconds after

```{r count-insula-sfn}

# LL12 insula #
results <- read.csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "sfn_submission","LL12_correlation_wilcox_test_hc_insula.csv"), header = F)
tmp <- results %>%
  filter(!is.na(V1)) %>%
  mutate(V5 = as.numeric( V5)) %>%
  mutate(pval_fdr = p.adjust(V5, "fdr"))
table(tmp$pval_fdr < .05)

insula_counter <- c(tmp$pval_fdr < .05)

# BJH021 insula #
results <- read.csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "sfn_submission","BJH021_correlation_wilcox_test_hc_insula.csv"), header = F)
tmp <- results %>%
  filter(!is.na(V1)) %>%
  mutate(V5 = as.numeric( V5)) %>%
  mutate(pval_fdr = p.adjust(V5, "fdr"))
table(tmp$pval_fdr < .05)
insula_counter <- c(insula_counter, tmp$pval_fdr < .05)

# BJH025 insula #
results <- read.csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "sfn_submission","BJH025_correlation_wilcox_test_hc_insula.csv"), header = F)
tmp <- results %>%
  filter(!is.na(V1)) %>%
  mutate(V5 = as.numeric( V5)) %>%
  mutate(pval_fdr = p.adjust(V5, "fdr"))
table(tmp$pval_fdr < .05)
insula_counter <- c(insula_counter, tmp$pval_fdr < .05)

# SLCH002 insula #
results <- read.csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "sfn_submission","SLCH002_correlation_wilcox_test_hc_insula.csv"), header = F)
tmp <- results %>%
  filter(!is.na(V1)) %>%
  mutate(V5 = as.numeric( V5)) %>%
  mutate(pval_fdr = p.adjust(V5, "fdr"))
table(tmp$pval_fdr < .05)
insula_counter <- c(insula_counter, tmp$pval_fdr < .05)


# BJH016 insula #
results <- read.csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "sfn_submission","BJH016_correlation_wilcox_test_hc_insula.csv"), header = F)
tmp <- results %>%
  filter(!is.na(V1)) %>%
  mutate(V5 = as.numeric( V5)) %>%
  mutate(pval_fdr = p.adjust(V5, "fdr"))
table(tmp$pval_fdr < .05)
insula_counter <- c(insula_counter, tmp$pval_fdr < .05)

print("Total")
table(insula_counter)  
all_results <- rbind(all_results,                      tibble(                        case = opts_current$get()$label,                        percent = mean(insula_counter)                      ))
```




# Last Away Analysis ~ 1 second after turn around (with no ghost trials!)

### dlPFC ~ Hippocampus Last Away 1 second after turn 

```{r count-hc-after-turn}


# LL10 hc #
print("LL10")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL10_correlation_turnaround_wilcox_test_dlpfc_hc.csv"), show_col_types = FALSE)
results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

hc_counter <- c(sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

# LL12 hc #
print("LL12")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL12_correlation_turnaround_wilcox_test_dlpfc_hc.csv"), show_col_types = FALSE)
results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

hc_counter <- c(hc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

# LL13 hc #
print("LL13")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL13_correlation_turnaround_wilcox_test_dlpfc_hc.csv"), show_col_types = FALSE)
results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

hc_counter <- c(hc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH021 hc #
print("BJH021")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH021_correlation_turnaround_wilcox_test_dlpfc_hc.csv"), show_col_types = FALSE)
results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

hc_counter <- c(hc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH025 hc #
print("BJH025")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH025_correlation_turnaround_wilcox_test_dlpfc_hc.csv"), show_col_types = FALSE)
results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

hc_counter <- c(hc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# SLCH002 hc #
print("SLCH002")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "SLCH002_correlation_turnaround_wilcox_test_dlpfc_hc.csv"), show_col_types = FALSE)
results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

hc_counter <- c(hc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))



# BJH016 hc #
print("BJH016")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH016_correlation_turnaround_wilcox_test_dlpfc_hc.csv"), show_col_types = FALSE)
results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

hc_counter <- c(hc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


print("Total")
table(hc_counter)  
all_results <- rbind(all_results,                      tibble(                        case = opts_current$get()$label,                        percent = mean(hc_counter)                      ))
```

### HC ~ Amyg Last Away 1 second after turn 

```{r count-amyg-after-turn}


# LL10 amyg #
print("LL10")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL10_correlation_turnaround_wilcox_test_hc_amyg.csv"), show_col_types = FALSE)
results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

amyg_counter <- c(sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

# LL12 amyg #
print("LL12")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL12_correlation_turnaround_wilcox_test_hc_amyg.csv"), show_col_types = FALSE)
results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

# LL13 amyg #
print("LL13")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL13_correlation_turnaround_wilcox_test_hc_amyg.csv"), show_col_types = FALSE)
results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))
print(table(results$pval_fdr < .05))

amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH021 amyg #
print("BJH021")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH021_correlation_turnaround_wilcox_test_hc_amyg.csv"), show_col_types = FALSE)
results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH025 amyg #
print("BJH025")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH025_correlation_turnaround_wilcox_test_hc_amyg.csv"), show_col_types = FALSE)
results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# SLCH002 amyg #
print("SLCH002")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "SLCH002_correlation_turnaround_wilcox_test_hc_amyg.csv"), show_col_types = FALSE)
results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))



# BJH016 amyg #
print("BJH016")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH016_correlation_turnaround_wilcox_test_hc_amyg.csv"), show_col_types = FALSE)
results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

print("Total")
table(amyg_counter)  
all_results <- rbind(all_results,                      tibble(                        case = opts_current$get()$label,                        percent = mean(amyg_counter)                      ))
```

### HC ~ OFC Last Away 1 second after turn 

```{r count-ofc-after-turn}


# LL10 ofc #
print("LL10")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL10_correlation_turnaround_wilcox_test_hc_ofc.csv"), show_col_types = FALSE)
results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

ofc_counter <- c(sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

# LL12 ofc #
print("LL12")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL12_correlation_turnaround_wilcox_test_hc_ofc.csv"), show_col_types = FALSE)
results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

ofc_counter <- c(ofc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

# LL13 ofc #
print("LL13")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL13_correlation_turnaround_wilcox_test_hc_ofc.csv"), show_col_types = FALSE)
results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

ofc_counter <- c(ofc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH021 ofc #
print("BJH021")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH021_correlation_turnaround_wilcox_test_hc_ofc.csv"), show_col_types = FALSE)
results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

ofc_counter <- c(ofc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH025 ofc #
print("BJH025")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH025_correlation_turnaround_wilcox_test_hc_ofc.csv"), show_col_types = FALSE)
results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

ofc_counter <- c(ofc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# SLCH002 ofc #
print("SLCH002")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "SLCH002_correlation_turnaround_wilcox_test_hc_ofc.csv"), show_col_types = FALSE)
results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

ofc_counter <- c(ofc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))



# BJH016 ofc #
print("BJH016")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH016_correlation_turnaround_wilcox_test_hc_ofc.csv"), show_col_types = FALSE)
results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

ofc_counter <- c(ofc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

print("Total")
table(ofc_counter)  
all_results <- rbind(all_results,                      tibble(                        case = opts_current$get()$label,                        percent = mean(ofc_counter)                      ))

```

### HC ~ Cing Last Away 1 second after turn 

```{r count-cing-after-turn}


# LL10 cing #
print("LL10")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL10_correlation_turnaround_wilcox_test_hc_cing.csv"), show_col_types = FALSE)
results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))


print(table(results$pval_fdr < .05))

cing_counter <- c(sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

# LL12 cing #
print("LL12")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL12_correlation_turnaround_wilcox_test_hc_cing.csv"), show_col_types = FALSE)
results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))


print(table(results$pval_fdr < .05))

cing_counter <- c(cing_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

# LL13 cing #
print("LL13")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL13_correlation_turnaround_wilcox_test_hc_cing.csv"), show_col_types = FALSE)
results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))


print(table(results$pval_fdr < .05))

cing_counter <- c(cing_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH021 cing #
print("BJH021")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH021_correlation_turnaround_wilcox_test_hc_cing.csv"), show_col_types = FALSE)
results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

cing_counter <- c(cing_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH025 cing #
print("BJH025")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH025_correlation_turnaround_wilcox_test_hc_cing.csv"), show_col_types = FALSE)
results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

cing_counter <- c(cing_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# SLCH002 cing #
print("SLCH002")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "SLCH002_correlation_turnaround_wilcox_test_hc_cing.csv"), show_col_types = FALSE)
results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))


print(table(results$pval_fdr < .05))

cing_counter <- c(cing_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))



# BJH016 cing #
print("BJH016")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH016_correlation_turnaround_wilcox_test_hc_cing.csv"), show_col_types = FALSE)
results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

cing_counter <- c(cing_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

print("Total")
table(cing_counter)  
all_results <- rbind(all_results,                      tibble(                        case = opts_current$get()$label,                        percent = mean(cing_counter)                      ))

```

### HC ~ Insula Last Away 1 second after turn 

```{r count-insula-after-turn}

# LL12 insula #
print("LL12")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL12_correlation_turnaround_wilcox_test_hc_insula.csv"), show_col_types = FALSE)
results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

insula_counter <- c(sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH021 insula #
print("BJH021")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH021_correlation_turnaround_wilcox_test_hc_insula.csv"), show_col_types = FALSE)
results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

insula_counter <- c(insula_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH025 insula #
print("BJH025")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH025_correlation_turnaround_wilcox_test_hc_insula.csv"), show_col_types = FALSE)
results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

insula_counter <- c(insula_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# SLCH002 insula #
print("SLCH002")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "SLCH002_correlation_turnaround_wilcox_test_hc_insula.csv"), show_col_types = FALSE)
results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

insula_counter <- c(insula_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))



# BJH016 insula #
print("BJH016")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH016_correlation_turnaround_wilcox_test_hc_insula.csv"), show_col_types = FALSE)
results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

insula_counter <- c(insula_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

print("Total")
table(insula_counter)  
all_results <- rbind(all_results,                      tibble(                        case = opts_current$get()$label,                        percent = mean(insula_counter)                      ))

```




# Last Away Analysis - 1 second before, 1 second after

### dlPFC ~ Hippocampus Last Away 1 second before, 1 second after

```{r count-hc-before-and-after}


# LL10 hc #
print("LL10")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL10_correlation_before_and_after_turnaround_wilcox_test_dlpfc_hc.csv"), show_col_types = FALSE)
results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

hc_counter <- c(sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

# LL12 hc #
print("LL12")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL12_correlation_before_and_after_turnaround_wilcox_test_dlpfc_hc.csv"), show_col_types = FALSE)
results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

hc_counter <- c(hc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

# LL13 hc #
print("LL13")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL13_correlation_before_and_after_turnaround_wilcox_test_dlpfc_hc.csv"), show_col_types = FALSE)
results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

hc_counter <- c(hc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH021 hc #
print("BJH021")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH021_correlation_before_and_after_turnaround_wilcox_test_dlpfc_hc.csv"), show_col_types = FALSE)
results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

hc_counter <- c(hc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH025 hc #
print("BJH025")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH025_correlation_before_and_after_turnaround_wilcox_test_dlpfc_hc.csv"), show_col_types = FALSE)
results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

hc_counter <- c(hc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# SLCH002 hc #
print("SLCH002")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "SLCH002_correlation_before_and_after_turnaround_wilcox_test_dlpfc_hc.csv"), show_col_types = FALSE)
results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

hc_counter <- c(hc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))



# BJH016 hc #
print("BJH016")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH016_correlation_before_and_after_turnaround_wilcox_test_dlpfc_hc.csv"), show_col_types = FALSE)
results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

hc_counter <- c(hc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


print("Total")
table(hc_counter)  
all_results <- rbind(all_results,                      tibble(                        case = opts_current$get()$label,                        percent = mean(hc_counter)                      ))
```

### dlpfc ~ Amyg Last Away 1 second before, 1 second after

```{r count-amyg-before-and-after}


# LL10 amyg #
print("LL10")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL10_correlation_before_and_after_turnaround_wilcox_test_dlpfc_amyg.csv"), show_col_types = FALSE)
results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

amyg_counter <- c(sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

# LL12 amyg #
print("LL12")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL12_correlation_before_and_after_turnaround_wilcox_test_dlpfc_amyg.csv"), show_col_types = FALSE)
results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

# LL13 amyg #
print("LL13")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL13_correlation_before_and_after_turnaround_wilcox_test_dlpfc_amyg.csv"), show_col_types = FALSE)
results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))
print(table(results$pval_fdr < .05))

amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH021 amyg #
print("BJH021")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH021_correlation_before_and_after_turnaround_wilcox_test_dlpfc_amyg.csv"), show_col_types = FALSE)
results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH025 amyg #
print("BJH025")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH025_correlation_before_and_after_turnaround_wilcox_test_dlpfc_amyg.csv"), show_col_types = FALSE)
results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# SLCH002 amyg #
print("SLCH002")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "SLCH002_correlation_before_and_after_turnaround_wilcox_test_dlpfc_amyg.csv"), show_col_types = FALSE)
results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))



# BJH016 amyg #
print("BJH016")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH016_correlation_before_and_after_turnaround_wilcox_test_dlpfc_amyg.csv"), show_col_types = FALSE)
results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

print("Total")
table(amyg_counter)  
all_results <- rbind(all_results,                      tibble(                        case = opts_current$get()$label,                        percent = mean(amyg_counter)                      ))
```

### dlpfc ~ OFC Last Away 1 second before, 1 second after

```{r count-ofc-before-and-after}


# LL10 ofc #
print("LL10")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL10_correlation_before_and_after_turnaround_wilcox_test_dlpfc_ofc.csv"), show_col_types = FALSE)
results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

ofc_counter <- c(sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

# LL12 ofc #
print("LL12")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL12_correlation_before_and_after_turnaround_wilcox_test_dlpfc_ofc.csv"), show_col_types = FALSE)
results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

ofc_counter <- c(ofc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

# LL13 ofc #
print("LL13")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL13_correlation_before_and_after_turnaround_wilcox_test_dlpfc_ofc.csv"), show_col_types = FALSE)
results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

ofc_counter <- c(ofc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH021 ofc #
print("BJH021")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH021_correlation_before_and_after_turnaround_wilcox_test_dlpfc_ofc.csv"), show_col_types = FALSE)
results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

ofc_counter <- c(ofc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH025 ofc #
print("BJH025")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH025_correlation_before_and_after_turnaround_wilcox_test_dlpfc_ofc.csv"), show_col_types = FALSE)
results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

ofc_counter <- c(ofc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# SLCH002 ofc #
print("SLCH002")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "SLCH002_correlation_before_and_after_turnaround_wilcox_test_dlpfc_ofc.csv"), show_col_types = FALSE)
results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

ofc_counter <- c(ofc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))



# BJH016 ofc #
print("BJH016")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH016_correlation_before_and_after_turnaround_wilcox_test_dlpfc_ofc.csv"), show_col_types = FALSE)
results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

ofc_counter <- c(ofc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

print("Total")
table(ofc_counter)  
all_results <- rbind(all_results,                      tibble(                        case = opts_current$get()$label,                        percent = mean(ofc_counter)                      ))

```

### dlpfc ~ Cing Last Away 1 second before, 1 second after

```{r count-cing-before-and-after}


# LL10 cing #
print("LL10")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL10_correlation_before_and_after_turnaround_wilcox_test_dlpfc_cing.csv"), show_col_types = FALSE)
results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))


print(table(results$pval_fdr < .05))

cing_counter <- c(sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

# LL12 cing #
print("LL12")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL12_correlation_before_and_after_turnaround_wilcox_test_dlpfc_cing.csv"), show_col_types = FALSE)
results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))


print(table(results$pval_fdr < .05))

cing_counter <- c(cing_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

# LL13 cing #
print("LL13")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL13_correlation_before_and_after_turnaround_wilcox_test_dlpfc_cing.csv"), show_col_types = FALSE)
results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))


print(table(results$pval_fdr < .05))

cing_counter <- c(cing_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH021 cing #
print("BJH021")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH021_correlation_before_and_after_turnaround_wilcox_test_dlpfc_cing.csv"), show_col_types = FALSE)
results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

cing_counter <- c(cing_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH025 cing #
print("BJH025")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH025_correlation_before_and_after_turnaround_wilcox_test_dlpfc_cing.csv"), show_col_types = FALSE)
results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

cing_counter <- c(cing_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# SLCH002 cing #
print("SLCH002")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "SLCH002_correlation_before_and_after_turnaround_wilcox_test_dlpfc_cing.csv"), show_col_types = FALSE)
results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))


print(table(results$pval_fdr < .05))

cing_counter <- c(cing_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))



# BJH016 cing #
print("BJH016")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH016_correlation_before_and_after_turnaround_wilcox_test_dlpfc_cing.csv"), show_col_types = FALSE)
results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

cing_counter <- c(cing_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

print("Total")
table(cing_counter)  
all_results <- rbind(all_results,                      tibble(                        case = opts_current$get()$label,                        percent = mean(cing_counter)                      ))

```

### dlpfc ~ Insula Last Away 1 second before, 1 second after

```{r count-insula-before-and-after}

# LL12 insula #
print("LL12")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL12_correlation_before_and_after_turnaround_wilcox_test_dlpfc_insula.csv"), show_col_types = FALSE)
results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

insula_counter <- c(sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH021 insula #
print("BJH021")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH021_correlation_before_and_after_turnaround_wilcox_test_dlpfc_insula.csv"), show_col_types = FALSE)
results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

insula_counter <- c(insula_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH025 insula #
print("BJH025")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH025_correlation_before_and_after_turnaround_wilcox_test_dlpfc_insula.csv"), show_col_types = FALSE)
results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

insula_counter <- c(insula_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# SLCH002 insula #
print("SLCH002")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "SLCH002_correlation_before_and_after_turnaround_wilcox_test_dlpfc_insula.csv"), show_col_types = FALSE)
results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

insula_counter <- c(insula_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))



# BJH016 insula #
print("BJH016")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH016_correlation_before_and_after_turnaround_wilcox_test_dlpfc_insula.csv"), show_col_types = FALSE)
results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

insula_counter <- c(insula_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

print("Total")
table(insula_counter)  
all_results <- rbind(all_results,                      tibble(                        case = opts_current$get()$label,                        percent = mean(insula_counter)                      ))

```


## HC 

### Hippocampus ~ OFC before_and_after_turnaround

```{r count-hc-ofc-before_and_after_turnaround}


# LL10 ofc #
print("LL10")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL10_correlation_before_and_after_turnaround_wilcox_test_hc_ofc.csv"),  
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

ofc_counter <- c(sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

# LL12 ofc #
print("LL12")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL12_correlation_before_and_after_turnaround_wilcox_test_hc_ofc.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

ofc_counter <- c(ofc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

# LL13 ofc #
print("LL13")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL13_correlation_before_and_after_turnaround_wilcox_test_hc_ofc.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))
print(table(results$pval_fdr < .05))

ofc_counter <- c(ofc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH021 ofc #
print("BJH021")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH021_correlation_before_and_after_turnaround_wilcox_test_hc_ofc.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

ofc_counter <- c(ofc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH025 ofc #
print("BJH025")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH025_correlation_before_and_after_turnaround_wilcox_test_hc_ofc.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

ofc_counter <- c(ofc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# SLCH002 ofc #
print("SLCH002")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "SLCH002_correlation_before_and_after_turnaround_wilcox_test_hc_ofc.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

ofc_counter <- c(ofc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))



# BJH016 ofc #
print("BJH016")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH016_correlation_before_and_after_turnaround_wilcox_test_hc_ofc.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

ofc_counter <- c(ofc_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

print("Total")
table(ofc_counter)  
all_results <- rbind(all_results,                      tibble(                        case = opts_current$get()$label,                        percent = mean(ofc_counter)                      ))

```

### Hippocampus ~ Cing before_and_after_turnaround

```{r count-hc-cing-before_and_after_turnaround}


# LL10 cing #
print("LL10")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL10_correlation_before_and_after_turnaround_wilcox_test_hc_cing.csv"),  
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

cing_counter <- c(sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

# LL12 cing #
print("LL12")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL12_correlation_before_and_after_turnaround_wilcox_test_hc_cing.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

cing_counter <- c(cing_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

# LL13 cing #
print("LL13")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL13_correlation_before_and_after_turnaround_wilcox_test_hc_cing.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

cing_counter <- c(cing_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH021 cing #
print("BJH021")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH021_correlation_before_and_after_turnaround_wilcox_test_hc_cing.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

cing_counter <- c(cing_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH025 cing #
print("BJH025")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH025_correlation_before_and_after_turnaround_wilcox_test_hc_cing.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

cing_counter <- c(cing_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# SLCH002 cing #
print("SLCH002")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "SLCH002_correlation_before_and_after_turnaround_wilcox_test_hc_cing.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

cing_counter <- c(cing_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))



# BJH016 cing #
print("BJH016")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH016_correlation_before_and_after_turnaround_wilcox_test_hc_cing.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

cing_counter <- c(cing_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

print("Total")
table(cing_counter)  
all_results <- rbind(all_results,                      tibble(                        case = opts_current$get()$label,                        percent = mean(cing_counter)                      ))

```

### Hippocampus ~ Amyg before_and_after_turnaround

```{r count-hc-amyg-before_and_after_turnaround}


# LL10 amyg #
print("LL10")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL10_correlation_before_and_after_turnaround_wilcox_test_hc_amyg.csv"),  
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

amyg_counter <- c(sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

# LL12 amyg #
print("LL12")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL12_correlation_before_and_after_turnaround_wilcox_test_hc_amyg.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

# LL13 amyg #
print("LL13")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL13_correlation_before_and_after_turnaround_wilcox_test_hc_amyg.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH021 amyg #
print("BJH021")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH021_correlation_before_and_after_turnaround_wilcox_test_hc_amyg.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH025 amyg #
print("BJH025")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH025_correlation_before_and_after_turnaround_wilcox_test_hc_amyg.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# SLCH002 amyg #
print("SLCH002")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "SLCH002_correlation_before_and_after_turnaround_wilcox_test_hc_amyg.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))



# BJH016 amyg #
print("BJH016")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH016_correlation_before_and_after_turnaround_wilcox_test_hc_amyg.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

print("Total")
table(amyg_counter)  
all_results <- rbind(all_results,                      tibble(                        case = opts_current$get()$label,                        percent = mean(amyg_counter)                      ))
```

### Hippocampus ~ Insula before_and_after_turnaround

```{r count-hc-insula-before_and_after_turnaround}



# LL12 insula #
print("LL12")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL12_correlation_before_and_after_turnaround_wilcox_test_hc_insula.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

insula_counter <- c(sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH021 insula #
print("BJH021")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH021_correlation_before_and_after_turnaround_wilcox_test_hc_insula.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

insula_counter <- c(insula_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH025 insula #
print("BJH025")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH025_correlation_before_and_after_turnaround_wilcox_test_hc_insula.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

insula_counter <- c(insula_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# SLCH002 insula #
print("SLCH002")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "SLCH002_correlation_before_and_after_turnaround_wilcox_test_hc_insula.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

insula_counter <- c(insula_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))



# BJH016 insula #
print("BJH016")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH016_correlation_before_and_after_turnaround_wilcox_test_hc_insula.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))
print(table(results$pval_fdr < .05))

insula_counter <- c(insula_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

print("Total")
table(insula_counter)  
all_results <- rbind(all_results,                      tibble(                        case = opts_current$get()$label,                        percent = mean(insula_counter)                      ))

```

## OFC 

### OFC ~ Cing before_and_after_turnaround

```{r count-ofc-cing-before_and_after_turnaround}


# LL10 cing #
print("LL10")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL10_correlation_before_and_after_turnaround_wilcox_test_ofc_cing.csv"),  
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

cing_counter <- c(sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

# LL12 cing #
print("LL12")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL12_correlation_before_and_after_turnaround_wilcox_test_ofc_cing.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

cing_counter <- c(cing_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

# LL13 cing #
print("LL13")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL13_correlation_before_and_after_turnaround_wilcox_test_ofc_cing.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

cing_counter <- c(cing_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH021 cing #
print("BJH021")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH021_correlation_before_and_after_turnaround_wilcox_test_ofc_cing.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

cing_counter <- c(cing_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH025 cing #
print("BJH025")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH025_correlation_before_and_after_turnaround_wilcox_test_ofc_cing.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

cing_counter <- c(cing_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# SLCH002 cing #
print("SLCH002")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "SLCH002_correlation_before_and_after_turnaround_wilcox_test_ofc_cing.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

cing_counter <- c(cing_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))



# BJH016 cing #
# print("BJH016")
# results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH016_correlation_before_and_after_turnaround_wilcox_test_ofc_cing.csv"), 
#                     col_names = T, col_types = cols(.default = "c", X1 = col_skip()))
# 
# results <- results %>%
#   mutate(pval_fdr = p.adjust(pval, "fdr"))
# 
# print(table(results$pval_fdr < .05))
# 
# cing_counter <- c(cing_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

print("Total")
table(cing_counter)  
all_results <- rbind(all_results,                      tibble(                        case = opts_current$get()$label,                        percent = mean(cing_counter)                      ))

```

### OFC ~ Amyg before_and_after_turnaround

```{r count-ofc-amyg-before_and_after_turnaround}


# LL10 amyg #
print("LL10")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL10_correlation_before_and_after_turnaround_wilcox_test_ofc_amyg.csv"),  
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

amyg_counter <- c(sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

# LL12 amyg #
print("LL12")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL12_correlation_before_and_after_turnaround_wilcox_test_ofc_amyg.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

# LL13 amyg #
print("LL13")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL13_correlation_before_and_after_turnaround_wilcox_test_ofc_amyg.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH021 amyg #
print("BJH021")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH021_correlation_before_and_after_turnaround_wilcox_test_ofc_amyg.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH025 amyg #
print("BJH025")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH025_correlation_before_and_after_turnaround_wilcox_test_ofc_amyg.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# SLCH002 amyg #
print("SLCH002")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "SLCH002_correlation_before_and_after_turnaround_wilcox_test_ofc_amyg.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))



# BJH016 amyg #
# print("BJH016")
# results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH016_correlation_before_and_after_turnaround_wilcox_test_ofc_amyg.csv"), 
#                     col_names = T, col_types = cols(.default = "c", X1 = col_skip()))
# 
# results <- results %>%
#   mutate(pval_fdr = p.adjust(pval, "fdr"))
# 
# print(table(results$pval_fdr < .05))
# 
# amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

print("Total")
table(amyg_counter)  
all_results <- rbind(all_results,                      tibble(                        case = opts_current$get()$label,                        percent = mean(amyg_counter)                      ))
```

### OFC ~ Insula before_and_after_turnaround

```{r count-ofc-insula-before_and_after_turnaround}



# LL12 insula #
print("LL12")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL12_correlation_before_and_after_turnaround_wilcox_test_ofc_insula.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

insula_counter <- c(sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH021 insula #
print("BJH021")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH021_correlation_before_and_after_turnaround_wilcox_test_ofc_insula.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

insula_counter <- c(insula_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH025 insula #
print("BJH025")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH025_correlation_before_and_after_turnaround_wilcox_test_ofc_insula.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

insula_counter <- c(insula_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# SLCH002 insula #
print("SLCH002")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "SLCH002_correlation_before_and_after_turnaround_wilcox_test_ofc_insula.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

insula_counter <- c(insula_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))



# BJH016 insula #
# print("BJH016")
# results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH016_correlation_before_and_after_turnaround_wilcox_test_ofc_insula.csv"), 
#                     col_names = T, col_types = cols(.default = "c", X1 = col_skip()))
# 
# results <- results  %>%
#   mutate(pval_fdr = p.adjust(pval, "fdr"))
# print(table(results$pval_fdr < .05))
# 
# insula_counter <- c(insula_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

print("Total")
table(insula_counter)  
all_results <- rbind(all_results,                      tibble(                        case = opts_current$get()$label,                        percent = mean(insula_counter)                      ))

```

## Ant. Cingulate

### Ant. Cingulate ~ Amyg before_and_after_turnaround

```{r count-cing-amyg-before_and_after_turnaround}


# LL10 amyg #
print("LL10")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL10_correlation_before_and_after_turnaround_wilcox_test_cing_amyg.csv"),  
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

amyg_counter <- c(sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

# LL12 amyg #
print("LL12")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL12_correlation_before_and_after_turnaround_wilcox_test_cing_amyg.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

# LL13 amyg #
print("LL13")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL13_correlation_before_and_after_turnaround_wilcox_test_cing_amyg.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH021 amyg #
print("BJH021")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH021_correlation_before_and_after_turnaround_wilcox_test_cing_amyg.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH025 amyg #
print("BJH025")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH025_correlation_before_and_after_turnaround_wilcox_test_cing_amyg.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# SLCH002 amyg #
print("SLCH002")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "SLCH002_correlation_before_and_after_turnaround_wilcox_test_cing_amyg.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))



# # BJH016 amyg #
# print("BJH016")
# results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH016_correlation_before_and_after_turnaround_wilcox_test_cing_amyg.csv"), 
#                     col_names = T, col_types = cols(.default = "c", X1 = col_skip()))
# 
# results <- results %>%
#   mutate(pval_fdr = p.adjust(pval, "fdr"))
# 
# print(table(results$pval_fdr < .05))
# 
# amyg_counter <- c(amyg_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

print("Total")
table(amyg_counter)  
all_results <- rbind(all_results,                      tibble(                        case = opts_current$get()$label,                        percent = mean(amyg_counter)                      ))
```

### Ant. Cingulate ~ Insula before_and_after_turnaround

```{r count-cing-insula-before_and_after_turnaround}



# LL12 insula #
print("LL12")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL12_correlation_before_and_after_turnaround_wilcox_test_cing_insula.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

insula_counter <- c(sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH021 insula #
print("BJH021")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH021_correlation_before_and_after_turnaround_wilcox_test_cing_insula.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

insula_counter <- c(insula_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH025 insula #
print("BJH025")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH025_correlation_before_and_after_turnaround_wilcox_test_cing_insula.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

insula_counter <- c(insula_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# SLCH002 insula #
print("SLCH002")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "SLCH002_correlation_before_and_after_turnaround_wilcox_test_cing_insula.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

insula_counter <- c(insula_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))



# BJH016 insula #
# print("BJH016")
# results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH016_correlation_before_and_after_turnaround_wilcox_test_cing_insula.csv"), 
#                     col_names = T, col_types = cols(.default = "c", X1 = col_skip()))
# 
# results <- results  %>%
#   mutate(pval_fdr = p.adjust(pval, "fdr"))
# print(table(results$pval_fdr < .05))
# 
# insula_counter <- c(insula_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

print("Total")
table(insula_counter)  
all_results <- rbind(all_results,                      tibble(                        case = opts_current$get()$label,                        percent = mean(insula_counter)                      ))

```

## Amygdala

### Amygdala ~ Insula before_and_after_turnaround

```{r count-amyg-insula-before_and_after_turnaround}



# LL12 insula #
print("LL12")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "LL12_correlation_before_and_after_turnaround_wilcox_test_amyg_insula.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

insula_counter <- c(sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH021 insula #
print("BJH021")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH021_correlation_before_and_after_turnaround_wilcox_test_amyg_insula.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

insula_counter <- c(insula_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# BJH025 insula #
print("BJH025")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH025_correlation_before_and_after_turnaround_wilcox_test_amyg_insula.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

insula_counter <- c(insula_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))


# SLCH002 insula #
print("SLCH002")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "SLCH002_correlation_before_and_after_turnaround_wilcox_test_amyg_insula.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))

print(table(results$pval_fdr < .05))

insula_counter <- c(insula_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))



# BJH016 insula #
print("BJH016")
results <- read_csv(path(here(), "data_mount/remote/pacman_behavior", "results", "cor_analysis", "BJH016_correlation_before_and_after_turnaround_wilcox_test_amyg_insula.csv"), 
                    col_names = T, col_types = cols(.default = "c", X1 = col_skip()))

results <- results  %>%
  mutate(pval_fdr = p.adjust(pval, "fdr"))
print(table(results$pval_fdr < .05))

insula_counter <- c(insula_counter, sum(as.numeric(results$pval_fdr < .05))/length(results$pval_fdr < .05))

print("Total")
table(insula_counter)  


all_results <- rbind(all_results,                      tibble(                        case = opts_current$get()$label,                        percent = mean(insula_counter)                      ))

```

```{r save-data}


write_csv(all_results, path(here(), "results", "sub_normalized_lfp_connectivity_summary.csv"))

```



